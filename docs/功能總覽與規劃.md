# 🏆 比賽管理系統 — 功能總覽與規劃

文件版本：2026-09-27（對應網站 v3.6.2）

線上網址：https://competition-manager-hazel.vercel.app

程式碼：https://github.com/RimuruTempest0417/competition-manager

本文件已重新整理：修正原本過時或標示錯誤的狀態、補上 v2.7.0～v2.13.0 的實際完成內容、新增「資料庫 Migration」「版本歷程」「測試方式」「已知限制」與「建議下一步」。

狀態用語：已完成／進行中／未開始。

## 一、使用者介面（瀏覽器端）

### 1.1 檢視與搜尋

| 功能 | 使用者角色 | 當前狀態 | 詳細說明 |
|---|---|---|---|
| Keywords 搜尋 + 日期篩選 | 所有用戶 | 已完成 | 支援名稱、地點、簡介關鍵字搜尋與特定日期篩選。 |
| 顯示發佈者 | 所有用戶 | 已完成 | 卡片顯示發佈者帳號與角色徽章。 |
| 賽事分類與標籤系統 | 所有用戶 | 已完成 (v2.7.0) | 10 種分類（含 emoji 與專屬色系）＋自由標籤（最多 10 個）。可分類／標籤／日期／關鍵字聯合篩選；分類清單由後端 GET /api/meta 提供單一真實來源。 |
| 日曆檢視模式 | 所有用戶 | 已完成 (v2.7.0) | 列表頁「📋 列表 / 📅 日曆」切換，月曆格線顯示每日賽程（依分類上色、跨日賽事展開到每一天、超過 3 場顯示「+N 更多」）。純手寫無外部套件、檢視選擇會被記住。 |
| 比賽狀態標籤與排序 | 所有用戶 | 已完成 (v2.19.0) | 賽事狀態機：**報名中／尚未開放報名／報名已截止／進行中／已結束**（另有日期未定、已刪除），由**後端依當下時間即時判定**並隨列表回傳，前端只負責顯示——不會出現「畫面說可報名、送出卻被拒」。附**狀態篩選列（含各狀態數量）**與倒數天數；狀態時間一到自動切換（讀取時計算，不需排程）。 |
| 美化 UI | 所有用戶 | 已完成 (v2.11.0) | 深淺色主題（自動＋手動三態切換 v2.6.0）、色票變數化；並完成手機介面優化：標題列單行化、篩選列手機改兩欄、彈窗改用 dvh 動態高度、觸控目標 ≥36px、輸入框 16px（避免 iOS 聚焦放大）、保留 iPhone 安全區域，390×844 與 360×640 實測橫向溢出 0px。 |
| 可安裝成手機 App（PWA） | 所有用戶 | 已完成 (v2.11.0) | 新增 manifest.json 與自製 App 圖示（192／512／maskable／apple-touch-icon），可「加入主畫面」以獨立視窗全螢幕使用。Android／桌面 Chrome 會出現「加到主畫面」按鈕。 |
| 深連結直達賽事 | 所有用戶 | 已完成 (v2.11.0) | 推播通知與分享連結帶 ?comp=<id>，開啟後自動切回列表、捲動並高亮該賽事；另支援 ?view=myregs／?view=notify。 |

### 1.2 線上報名與組隊（v2.9.0 新增）

| 功能 | 使用者角色 | 當前狀態 | 詳細說明 |
|---|---|---|---|
| 報名參加比賽 | 所有用戶（需登入） | 已完成 (v2.19.0) | 卡片「📝 報名」按鈕；未登入點擊會先開啟登入／註冊視窗。**按鈕是否可按完全照後端回傳的狀態**（`can_register`）：不可報名時按鈕直接寫出原因（「🔒 報名將於 … 開始」／「🔒 報名已於 … 截止」／「🔒 此賽事已開始，報名已截止」／「🔒 此賽事已結束」／「🔒 名額已滿」），即使硬送也會被後端 400 擋下。 |
| 賽事狀態與狀態篩選 | 所有用戶 | 已完成 (v2.19.0) | 列表上方「全部／報名中／尚未開放／報名已截止／進行中／已結束」籤（附各狀態數量，手機可橫向滑動），與關鍵字／分類／標籤／日期篩選並用；狀態由後端依當下時間判定，時間到自動切換。 |
| 我的報名 | 所有用戶（需登入） | 已完成 (v2.20.0) | 選單「📝 我的報名」：查看自己的報名（賽事日期／地點／隊伍／備註）並可自行取消（軟刪除，取消後可重新報名）。**每筆顯示報名狀態**：待審核／已核准／**候補 第 N 位**／未錄取（含拒絕原因）；未錄取與已被取消的報名不再顯示取消按鈕。 |
| 報名審核 | 管理員以上（審核）、所有用戶（送出） | 已完成 (v2.20.0) | 賽事可開啟「⏳ 報名需審核」：報名後狀態是**待審核**，管理員在「報名／隊伍」視窗逐筆「✅ 核准」「❌ 拒絕」（拒絕可寫原因，會通知對方並寫稽核 `REGISTER_APPROVED`／`REGISTER_REJECTED`）。**核准時名額已滿會擋（400）**，不能靠審核超收。待審核的人**不會**出現在「未編排報名者」名單（審核前不能先佔隊伍位）。 |
| 候補名單與自動遞補 | 管理員以上（設定與手動遞補）、所有用戶（排隊） | 已完成 (v2.20.0) | 賽事可開啟「🕒 額滿開放候補」：名額滿了仍可報名並排進候補（回報**第幾順位**，卡片提示「額滿，報名排候補（已有 N 人候補）」、按鈕變「📝 報名（排候補）」）。**有人取消（或取消／改拒絕已核准者）讓出名額 → 第一位候補自動遞補**並推播通知（`push_log` 記 `waitlist_promoted`、稽核 `AUTO_PROMOTE_WAITLIST`）；需審核的賽事遞補後仍是待審核。管理員另有「⬆️ 遞補下一位」（`PROMOTE_WAITLIST`，名額已滿時停用並說明原因）。 |
| 指定遞補與候補順位調整 | 管理員以上 | 已完成 (v2.21.0) | **指定遞補（不照順位）**：候補每一列有「⬆️ 遞補」可跳過排前面的人直接遞補指定的那一位（`POST /api/registrations/:id/promote`），**被跳過的人完全不受影響**（不像「先取消再遞補」是破壞性動作）；`review_note` 記「指定遞補（原第 N 順位）」、稽核 `PROMOTE_WAITLIST` 註明未照順位。**手動調整順序**：每列 `↑`／`↓` 與隔壁對調，送出完整順序寫入 `registrations.waitlist_order`（`POST /api/competitions/:id/waitlist/reorder`）；名單變動（漏一筆／多一筆／重複）整批拒絕且不改資料。排序規則：手動數字優先 → 只有一筆排過的在前面 → 都沒排過照報名時間，**新報名的人一律排到最後**。標題下方顯示順位來源（依報名時間／管理員手動排定），名額已滿時遞補按鈕停用；「我的報名」的順位同步。 |
| 候補排序工具與異動紀錄 | 管理員以上 | 已完成 (v2.22.0) | **拖拉排序**（候補列左側 `⠿` 把手，用 pointer 事件、手機可拖；拖動只動畫面、放手才送完整順序）、**「第 N 位」下拉**（一次搬到任意順位，鍵盤可用）、**「🕘 順位異動」面板**（`GET /api/competitions/:id/waitlist/history`，讀稽核日誌：調整順位／遞補／審核／通知設定／取消，含操作者與內容，最多 100 筆）。**每賽事的「遞補通知」開關**（`POST /api/competitions/:id/waitlist/notify`，關掉時遞補照常生效但完全不碰推播，三條遞補路徑都遵守，稽核記「依賽事設定未通知」）。**匯出報名名單 CSV 補「狀態」「候補順位」**；並修好 `CMCSV.stringify()` 物件列會產生空行 CSV 的老 bug。 |
| 候補批次操作與紀錄分頁 | 管理員以上 | 已完成 (v2.23.0) | **多選一次搬多筆**：候補列加勾選框，勾選後出現批次列（已選 N 筆／搬到第 N 位／搬過去／清除選取）；搬完的相對順序＝名單原順序，一次搬 N 筆時目標位置只到「人數 − N + 1」，搬完勾選自動清空；名單被別人改動時整批拒絕並要求重新整理（沿用 v2.21.0 的完整順序驗證，無新端點）。**異動紀錄往回翻**：「⬇️ 載入更早」用 offset 接續，說明顯示「已顯示 N 筆／已顯示全部 N 筆／達 500 筆上限」，分頁不重複也不漏。 |
| 賽事複製與週期性 | 管理員以上 | 已完成 (v2.24.0) | **📄 複製賽事**：一鍵產生新場次，照抄設定（地點／日期／時間／說明／分類與標籤／隊伍設定／報名窗／需審核／開放候補／遞補通知），**不搬報名、隊伍與海報**；名稱加「（複製）」且不重複疊加；複製品不帶週期、不會被排程當成系列成員。**🔁 週期性賽事**：設定每週／每兩週／每月與「重複到哪一天」（留空＝一直重複），卡片按鈕顯示目前週期；每日排程與面板「⬇️ 立即建立下一場」走同一份規則——下一場＝系列最後一場往後推一個週期（每月遇缺日夾月底）、只提前 30 天內建立、**一次只排一場**、報名時間一起往後挪、超過結束日就停、週期落後會回報請人工處理；週期屬整個系列，改一場會同步其他場次。migration `2026-09-26-v2.24.0-recurrence.sql`。 |
| 推播時間與事件可設定 | 管理員以上（設定）、已訂閱推播的用戶（收到） | 已完成 (v2.25.0) | 導覽選單「⏰ 推播設定」：**每日摘要總開關**、**發送時間**（澳門時間，預設 09:00）、**摘要要包含哪些事件**（新發布賽事／即將開賽提醒）、**即時通知的事件**（報名審核結果／候補遞補通知）可各自開關；儲存後畫面回報**每一項的前後變化**並回讀確認，留稽核（更新推播設定，記前後值）。伺服器每天檢查兩次（Vercel cron 01:00／13:00 UTC），**只有「當地日期還沒發過且已過設定時間」才發**，設定時間若在兩次檢查之間當天稍晚會**補發一次**；沒訂閱者／沒到期賽事／事件全關都會明確回報原因。被關掉的即時通知**連推播訂閱都不查**（不是查了才不送），審核／遞補／稽核流程照常；**讀不到設定一律視為開啟**（情願多發不漏發）並在面板提示降級。設定存在既有 `app_settings`，**不需 migration**。 |
| 營運儀表板 | 管理員以上 | 已完成 (v3.0.0) | 選單「📊 營運儀表板」：六個關鍵數字（賽事總數含報名中／已結束、報名總數含近 7 天新增、候補中含待審核／已核准、使用者含啟用與近 30 天登入、近 30 天未處理錯誤、推播成功率含失敗筆數）＋三張手寫 SVG 圖（報名趨勢、錯誤趨勢、賽事分類分佈；可切 7／14／30 天）＋效能現況（海報縮圖省下多少、分頁上限、**逐區塊實際查詢耗時**、索引數量）。統計規則唯一真實來源 `public/js/stats.js`（前後端共用，圖上的數字與 API 是同一套算法）。**只回聚合數字**（沒有任何帳號／email／單筆報名內容）；每個區塊各自 try／catch，缺表只列在 `unavailable_sections` 並在畫面說明，不讓整個儀表板壞掉；結果快取 60 秒、`?fresh=1` 強制重算；一般使用者與訪客 403。 |
| 列表分頁與查詢優化 | 所有人（讀） | 已完成 (v3.0.0) | **分頁是 opt-in**：`GET /api/competitions?limit=50&offset=100` 回 `{ items, total, limit, offset, has_more, returned, paged_by }`，**沒帶 `limit` 的請求行為完全不變**（仍回原本的陣列）；報名名單同樣支援（回應多一個 `page` 物件，`registrations` 只回那一頁、`total` 是全部筆數）。單次上限賽事 100 筆／名單 200 筆，壞參數退回安全預設而不是 400。`paged_by` 誠實標示 `db`（資料庫切片）或 `memory`（`?state=` 是推導狀態、`?status=` 要先正規化，資料庫無法可靠過濾）。**實際優化**：分頁時報名人數只撈這一頁賽事（原本掃整張表），成本從 O(全部報名) 變成 O(本頁賽事)；一般使用者分頁的 `total` 只算自己看得到的筆數。新增 `migrations/2026-09-27-v3.0.0-perf.sql` 索引與查詢次數守門測試（列表固定 3 次查詢、不隨賽事數量成長）。 |
| 名單與成績列印版 | 所有人（列印已公布成績）；管理員以上（列印報名名單） | 已完成 (v3.2.0) | **現場要用的那張紙**：賽事卡片「🖨️ 列印名單」把**已錄取**的人排成正取名單（候補自成一段並標示「候補 1、候補 2」依遞補順位，不會被誤當正取點名；未錄取／已取消不印），紙上有賽事名稱／時間（含結束）／地點／類別／列印時間／摘要句（共幾人．正取幾人．候補幾人．尚餘幾個名額）；可切換**包含候補**、**包含待審核**、**簽到欄**（手寫打勾）、**橫向**。「🖨️ 列印成績」印名次表（並列名次照原值 1,1,3，未到場顯示「—」）。預覽視窗按「列印 / 另存 PDF」用瀏覽器列印，**列印時只印那張紙**（`@page A4`、表頭每頁重複、App 介面全隱藏）。規則唯一真實來源 `public/js/printdoc.js`（UMD，預覽與列印與測試共用）；樣式在 `public/css/print.css`（CSP `style-src 'self'`，不用 inline style）。紙本會離開系統，所以只放姓名／隊伍／成績等現場必要資訊，不含 email／密碼／雜湊／token。 |
| 賽事規程 PDF 附件 | 所有人（讀取，不需登入）；管理員以上（上傳／移除） | 已完成 (v3.3.0) | **規則文件不必再放外部連結**：賽事卡片上多一個「📘 規程」按鈕（另開新視窗用瀏覽器開啟 PDF，也可另存），編輯賽事時可直接上傳／更換／移除（一場一份，重新上傳即覆蓋）。公開給所有人（參賽者、家長、現場人員都看得到）；上傳／移除只有管理員以上，且都留稽核。存法與海報一致（base64 存資料庫、自家端點供應，CSP 不必放寬、不需要外部物件儲存）。檔案一定要真的是 PDF（檢查檔頭 `%PDF-`，不是看副檔名）、上限 3MB。 |
| 成績與結果 | 所有人（看已公布）；管理員以上（登錄與公布） | 已完成 (v3.1.0) | **賽後那一段接起來**：賽事卡片「🏆 成績登錄」帶出這場**已核准**的報名名單（未審核／已拒絕／已取消的人不會出現），每列填成績（自由文字，如 `12:34.56`／`85`／`3–2`）、狀態（完賽／未完賽／未出賽／取消資格／其他）、名次、備註；「🔢 自動排名」一鍵算名次（時間類越小越好、分數類越大越好，**同分並列 1,1,3**）；**只有完賽且成績看得懂的人進名次**，看不懂的成績不會被當 0 排第一名。先「💾 儲存草稿」（前台看不到）再「🏁 公布結果」（可同時推播通知參賽者、可重新公布、可取消公布退回草稿）。前台已公布的賽事有「🏆 成績」：前三名領獎台＋完整名次表＋自己的那一列標記「（你）」＋匯出 CSV；「我的報名」直接顯示 `🏆 成績：🥈 第 2 名 · 12:30`。**未公布時對外連統計都不給**（避免反推名次）。公布前檢查（缺成績／名次重複／名次跳號）只提示不擋人。migration `2026-09-27-v3.1.0-results.sql`；規則唯一真實來源 `public/js/results.js`。 |
| 海報縮圖與列表載入優化 | 所有人（看）；管理員以上（上傳） | 已完成 (v3.0.0) | 上傳海報時瀏覽器用 canvas **同時產生縮圖**（最寬 480px JPEG）一起送出（**沒有引入任何伺服器端影像套件**）。`GET /api/competitions/:id/poster?variant=thumb` 回縮圖，**沒有縮圖就自動退回原圖**並用 `X-Poster-Variant: full-fallback` 標示（舊海報不需回填）。列表回 `poster_thumb_url`，卡片縮圖用 `loading="lazy"` ＋ `decoding="async"` 且可點開完整海報——**視窗外的卡片完全不下載圖片**；分享／列印仍用原圖。縮圖上限 250KB（超過只存原圖並在回應說明原因）、換海報會清掉舊縮圖。 |
| 場地地圖連結 | 所有人（看）；管理員以上（設定） | 已完成 (v2.27.0) | 賽事表單「地圖連結 (選填)」；卡片地點旁「🗺️ 地圖」連結（另開新頁＋`rel=noopener`）。**沒填就用地址自動產生** Google 地圖搜尋連結並標示「（用地址搜尋）」，**舊賽事不用補資料就有地圖可按**；有填以自訂連結為準（`www.google.com/maps/...` 之類沒帶協定會自動補 `https://`）。只接受 `http／https`；`javascript:`／`data:`／把地址整串貼進地圖欄位一律 **400＋說明原因**（不靜默丟掉、也不會偷偷建立賽事）。更新：留空＝清掉自訂連結回到自動產生、完全沒帶欄位＝不動既有值。複製賽事與週期自動建立的下一場沿用同一場地連結。規則唯一真實來源 `public/js/venue.js`（UMD 前後端共用）。 |
| 賽事工作人員指派 | 所有人（看清單）；管理員以上（指派／移除） | 已完成 (v2.27.0) | 每場賽事指派工作人員，角色五種：**裁判／記錄／攝影／醫護／其他**（`public/js/staff.js` 為唯一真實來源）。**同一場同一人只有一個角色**（再指派＝改角色；稽核寫出「裁判 → 攝影」前後差異），可加備註（200 字內；沒帶＝不動既有備註、帶空字串才清掉）。卡片徽章「🧑‍⚖️ 工作人員 N」＋面板（角色人數摘要、每列「⚖️ 裁判 王小明（第三場地）」、由誰指派）。**清單公開但只回角色與帳號名稱**（不含 email／權限／token）；指派與移除僅管理員以上並留稽核 `ASSIGN_STAFF`／`REMOVE_STAFF`；停用帳號不可指派；帳號被刪除時該筆仍列出並顯示「（已刪除）」。migration `2026-09-26-v2.27.0-venue-staff.sql`。 |
| 推播失敗可視與重送 | 管理員以上 | 已完成 (v2.26.0) | 「📤 推播紀錄」每筆顯示成功／失敗則數、**失敗原因**（`HTTP 410` 之類，最多 500 字）、**當時送出的內容**與**重送次數**；失敗的以紅框標示並提供「**重送給原本的對象**」。規則：單人通知只送他那筆訂閱、公告只送當初名單上的人（`target_user_id`／`target_user_ids`，不是全站廣播）；重送是**手動指令**，不受「⏰ 推播設定」的自動開關影響；重送結果（成功／失敗／清除失效訂閱）寫回同一筆紀錄（`resend_count`／`resend_at`／`resend_sent_count`）並留稽核 `RESEND_PUSH`（寫明送給誰）；訂閱回 `404／410` 時順手標記該訂閱失效；v2.26.0 之前的舊紀錄沒有內容可按，回 400 說明。migration `2026-09-26-v2.26.0-push-log-detail.sql`（未執行時：紀錄照常可看、端點回 `detail_ready:false`＋檔名、重送回 503）。 |
| 站內公告／訊息中心 | 登入使用者（讀、訂閱分類）；管理員以上（發布與管理） | 已完成 (v2.26.0) | 導覽選單「📣 公告中心」＋**未讀徽章**（99+ 上限），點公告即標記已讀、「全部標記已讀」；可勾選**訂閱的公告分類**（賽事分類同一套 10 類）並即時反映在看得到的公告。管理員以上另有「管理公告」：發布／編輯、**置頂**、**預約發布時間**、結束時間、**上架／下架**（下架只是不顯示、資料保留），以及**每則的已讀人數**（已讀 N／有效使用者 M）。對象：全部／管理員以上／特定分類訂閱者；發布可勾「同時發送推播」（`push_event_announce` 站台開關關掉時公告照發、推播跳過並說明原因，只推給符合對象使用者的訂閱）。**可見性**：上架中＋（管理員一律看得到／對象符合）——管理員看得到全部上架中的公告，但**通知仍嚴格照對象送**；規則唯一真實來源 `public/js/announcements.js`（UMD，後端 `require` 同一份）。migration `2026-09-26-v2.26.0-announcements.sql`（`announcements`／`announcement_reads`／`admin_users.announce_categories`＋RLS；未執行時列表回 `schema_ready:false`＋檔名、發布／修改／標記已讀回 503）。 |
| 名額規則（唯一真實來源） | — | 已完成 (v2.20.0) | **佔名額＝已核准＋待審核**，候補不佔名額。規則寫在 `public/js/competition-state.js`（v2.19.0 起前後端共用的同一份模組），後端報名／審核／遞補與前端卡片、統計、提示都走同一套，不會出現「畫面說可以、送出被拒」。報名卡片與 `/api/registration-counts` 的人數只算佔名額者，另附候補人數。 |
| 組隊比賽 | 所有用戶 | 已完成 (v2.9.0) | 賽事可勾選「👥 組隊比賽」並設定每隊人數上限；報名時必須填寫隊伍名稱。 |
| 編排比賽隊伍 | 管理員以上 | 已完成 (v2.9.0) | 卡片「👥 報名／隊伍」開啟編排視窗：建立隊伍、把報名者編入／移動隊伍、檢視未編排名單；編排會檢查「同一場賽事」與隊伍人數上限。 |
| 新增／刪除隊伍與隊員編排 | 超級管理員以上 | 已完成 (v2.9.0) | 可刪除隊伍（隊員自動退回未編排）與移除隊員。權限於後端驗證，前端亦依角色隱藏按鈕（一般用戶完全看不到管理按鈕）。 |
| 報名名單匯出 CSV | 管理員以上 | 已完成 (v2.11.0) | 隊伍編排視窗可匯出該場報名名單（姓名／隊伍／備註／報名時間），方便現場報到與計分。 |
| 剩餘名額顯示 | 所有用戶 | 已完成 (v2.11.0) | 卡片顯示「👥 3 / 20 人」＋「剩 17 個名額」，額滿顯示「🔒 名額已滿」。 |
| 報名截止與名額上限 | 管理員以上（設定） | 已完成 (v2.19.0) | 賽事可設定**報名開始時間**與**報名截止時間**（到分鐘，`datetime-local`；截止時間會自動同步舊的「報名截止日」欄位）與名額上限；表單下方有**即時狀態預覽**，用與後端相同的規則顯示「這樣設定會是什麼狀態」。仍保留手動「開放報名中」開關（可用來延長或提前關閉報名）。 |
| 編輯報名情況 / 編輯報名人員 | 管理員 / 超級管理員 | 已完成 (v2.9.0) | 即上列「編排比賽隊伍」與「新增／刪除隊伍與隊員編排」。 |

### 1.3 通知與訂閱

| 功能 | 使用者角色 | 當前狀態 | 詳細說明 |
|---|---|---|---|
| 訊息提醒（本機通知） | 所有用戶 | 已完成 (v2.8.0) | 卡片「🔕 訂閱提醒」；開賽前 24 小時內跳出提醒。網頁開啟時檢查（開啟、切回分頁、每 5 分鐘）。 |
| 推播訂閱服務（Web Push） | 所有用戶 | 已完成 (v2.10.0) | 「🔔 通知設定 → 📲 推播訂閱」：即使完全關閉網頁也能收到「新賽事發布」與「開賽前 24 小時」推播。伺服器每日由 Vercel Cron 發送；金鑰自動產生並存於資料庫，私鑰不進程式碼。 |
| 電子郵件通知 / 訂閱賽事 | 所有用戶 | 未開始 | 需要外部寄信服務（如 Resend／SendGrid）與帳號金鑰，尚未實作。詳見「建議下一步」。 |
| Supabase Realtime 即時同步 | 所有用戶 | 未開始 | 目前以手動重新載入取得最新資料；Realtime 需前端持有金鑰與 WebSocket 連線，與現行「前端不直接連資料庫」的架構衝突，需先評估。 |

### 1.4 分享與海報

| 功能 | 使用者角色 | 當前狀態 | 詳細說明 |
|---|---|---|---|
| 一鍵複製比賽資訊 / 分享連結 | 所有用戶 | 已完成 | 卡片「📋 複製文字」自動生成格式化賽事簡介並複製到剪貼簿。 |
| 海報一鍵生成（自動） | 所有用戶 | 已完成 | 「🖼️ 分享海報」以 Canvas 繪製宣傳海報，可複製或下載 PNG。 |
| 手動上傳海報（取代自動生成） | 管理員以上（上傳）／所有用戶（檢視） | 已完成 (v2.10.0) | 發佈／編輯表單可上傳 JPG／PNG／WebP（前端先縮圖，最長邊 1600px），卡片顯示「🖼️ 自訂海報」徽章，分享海報視窗改顯示上傳的圖；未上傳時仍用自動生成款式。 |
| 加入行事曆（行程檔 / Google 日曆） | 所有用戶（需登入） | 已完成 (v2.11.0) | 「我的報名」每筆可下載標準 .ics 行程檔（含賽事前一天提醒）或一鍵加入 Google 日曆，純前端產生、無外部依賴。 |

### 1.5 管理與發佈

| 功能 | 使用者角色 | 當前狀態 | 詳細說明 |
|---|---|---|---|
| 手動發佈與選填欄位 | 管理員 | 已完成 | 網頁表單發佈賽事，地點、日期、時間彈性選填。 |
| 24 小時制時間驗證（HH:mm） | 管理員 | 已完成 | 網頁表單輸入防呆正則驗證。 |
| 新增結束時間 | 管理員 | 已完成 | 支援 end_date / end_time。 |
| 修改已發佈數據 | 管理員 | 已完成 | 卡片「編輯」彈出視窗更新內容。 |
| 手動刪除比賽（軟刪除） | 管理員 | 已完成 | 卡片刪除按鈕，僅標記 is_deleted，前端自動過濾。 |
| 回復已刪除比賽 | 管理員 | 已完成 | 資源回收桶可還原。 |
| 永久刪除比賽 | 超級管理員 | 已完成 | 資源回收桶內永久刪除。 |
| 一鍵匯入 / 匯出 CSV | 管理員 | 已完成 (v2.8.0) | 「📥 CSV 匯入 / 匯出」：下載範本、批次匯入（Excel 另存的 CSV 或直接複製的 Tab 分隔內容，欄位順序不拘、表頭中英文皆可）、匯出備份（UTF-8 含 BOM）。匯入前先預覽，重複（同名同日）自動跳過並回報列號。 |

### 1.6 安全與權限

| 功能 | 使用者角色 | 當前狀態 | 詳細說明 |
|---|---|---|---|
| 網頁端身份切換（登入） | 所有用戶 | 已完成 | 網頁右上角登入入口。 |
| 普通用戶（user）自行註冊 | 所有用戶 | 已完成 (v2.9.0) | 登入視窗「註冊新帳號」，註冊成功即自動登入。普通用戶只能報名與管理自己的報名，看不到發佈與管理工具。 |
| 向 super admin 發送請求 | 所有用戶 | 已完成 | 發送任意文字訊息。 |
| 修改自己密碼 | 所有用戶（需登入） | 已完成 | 於 v2.9.0 起改用 crypto.scrypt 加鹽雜湊儲存；v2.12.1 起舊帳號只要登入一次就會自動升級為雜湊（不必再手動改密碼），並可用批次腳本一次處理尚未登入的舊帳號。 |
| 帳號管理面板 | 超級管理員 | 已完成 | 前端動態新增與刪除其他管理員帳號。 |
| 錯誤日誌與監控 | 超級管理員以上 | 已完成 (v2.12.0) | 日誌介面支援嚴重程度（錯誤／警告／資訊）、關鍵字搜尋、只看未處理、標記已處理（記錄處理人）、匯出 CSV、清理 N 天前；並在開啟時檢查「日誌系統本身」是否寫入失敗。已修復「系統出錯卻沒寫入日誌」：寫入失敗不再靜默，且補齊 13 條原本沒有記錄的錯誤路徑。 |
| 稽核動作名稱與紀錄完整度 | 超級管理員以上 | 已完成 (v2.19.0) | 動作下拉選單的每個動作都有中文名稱（線上 48 個動作全部對得上，`npm run check:audit-labels` 可隨時複查），不會再出現只有英文的孤兒動作。 |
| 稽核紀錄完整度（守門測試） | — | 已完成 (v2.19.0) | 補上「登出」`LOGOUT`（新增 `POST /api/auth/logout`，登出真的留紀錄）與「實際發送推播」`SEND_PUSH`（原本只有設定變更會記錄）。新增 `tests/audit-actions.test.js` 比對「稽核選單宣告的動作」＝「程式 `logAudit` 實際會寫的動作」——這種不一致本身**不會有任何錯誤訊息**（選單多一個動作就是永遠查不到、程式多寫一個動作就是選單裡找不到），只能靠測試守住。
| 推播發送紀錄 | 管理員以上 | 已完成 (v2.12.0) | 「📤 推播紀錄」顯示每日排程實際發送的推播（賽事、類型、成功數量、時間）。 |
| 角色權限分層 | — | 已完成 (v2.11.0 強化) | user（報名）→ admin（發佈／編輯／檢視報名名單／編排隊伍）→ super_admin（永久刪除、刪除隊伍與移除隊員）→ web_owner（最高權限）。實際角色一律以資料庫當下內容為準。 |
| 使用者管理（帳號建立與管理） | 管理員以上 | 已完成 (v2.12.0) | 「👥 使用者管理」可建立帳號、重設密碼、改帳號名稱、停用／啟用、刪除。角色階梯：管理員可建立普通用戶（user）與測試帳號；超級管理員可再建立管理員；網站擁有者不限。只有網站擁有者能調整他人角色。保護機制：不能改自己的角色、不能停用／刪除自己、不能刪除網站擁有者、至少保留一位網站擁有者、不能管理同級或更高權限者。前端選單不提供越權選項，且後端一律重新驗證（前端被繞過也會被擋）。 |
| 帳號安全 | 管理員以上（設定）／所有用戶（登入） | 已完成 (v2.12.0) | 登入連續失敗 5 次鎖定 15 分鐘、停用帳號不得登入、記錄最後登入時間、無效權杖留下警告級紀錄（同一 IP 每分鐘最多一筆）。 |
| 選單權限單一來源 | — | 已完成 (v2.11.0) | 修正「先登入管理員再登出，選單仍留有 CSV 匯入／匯出」的問題：改為單一權限表 CM_MENU_PERMISSIONS（guest／user／test／admin／super_admin／web_owner）統一控制，並在 CSV 相關函式（開啟視窗、下載範本、匯出、匯入）加上 isAdminUser() 雙重防護。 |

## 二、後端、基礎設施與安全

| 分類 / 範疇 | 技術目標 / 機制 | 當前狀態 | 詳細說明 |
|---|---|---|---|
| 底層架構 | Express RESTful API 基礎建設 | 已完成 | GET / POST / PUT / DELETE 端點與 JSON 資料處理、錯誤處理中間件。 |
| 雲端基礎設施 | Supabase (PostgreSQL) 雲端資料庫 | 已完成 | 取代 SQLite，資料跨裝置永久保存。 |
| 部署 | Vercel 自動化部署與環境變數注入 | 已完成 | SUPABASE_URL / SUPABASE_KEY / JWT_SECRET 皆由環境變數注入，不進 Git。（早期 Render 站台已停更。） |
| 排程 | Vercel Cron 每日推播 | 已完成 (v2.10.0) | vercel.json 設定每日呼叫 GET /api/cron/reminders 發送推播（push_log 去重、失效訂閱自動停用）。 |
| 排程安全 | cron 端點授權 | 已完成 (v2.11.1) | 修正原本「未設定 CRON_SECRET 就只做限流、等於任何人可觸發」的漏洞：未設定時 production 一律回 503（並提示去設定環境變數），有設定時以 crypto.timingSafeEqual 固定時間比較。待辦：在 Vercel 加入 CRON_SECRET，否則每日推播排程維持停用。 |
| 自動化維運 | 過期數據自動排查 | 進行中 | push_log 已可去重與清理；賽事歸檔／過期清理尚未實作，但排程基礎設施已具備，可直接掛在同一支 cron。 |
| 外部數據接入 | 自動化網路爬蟲（Scraper Service) | 未開始 | 需背景爬蟲服務與目標網站授權評估。 |
| 權限與驗證 | JWT 身份驗證 | 已完成 | Token 有效期 12 小時，payload 僅 sub / username / role（不含密碼）。 |
| 權限與驗證 | 後端 API 權限中間件 | 已完成 (v2.9.0 擴充) | authenticateToken + requireAdmin / requireSuperAdmin（ADMIN_ROLES／SUPER_ADMIN_ROLES 白名單），所有寫入端點皆有把關。 |
| 密碼安全 | 密碼雜湊儲存 | 已完成 (v2.12.1) | crypto.scrypt 加鹽雜湊（lib/passwords.js，伺服器與腳本共用）+ timingSafeEqual 比對。v2.12.1 前舊帳號的密碼其實是明碼存在資料庫裡（已全面升級為雜湊）：登入成功時自動升級、批次腳本 scripts/hash-legacy-passwords.js（寫入前自我驗證、寫入後複驗、不輸出任何密碼）、/api/admin/error-logs/health 可查尚待升級數量。 |
| 稽核 | 操作日誌與紀錄 | 已完成 | 登入／登出與關鍵資料變更（含報名、隊伍編排、海報上傳）皆寫入 audit_logs，自動解析 User-Agent 裝置類型。**v2.16.0 起可依使用者／動作／時間區間／關鍵字篩選、分頁瀏覽、匯出 CSV（含公式注入防護）、依保留天數清理（最少 30 天、可先預覽、預設不自動刪除）。** |
| 稽核 | Bug 回報 | 已完成 | 前端可回報錯誤，寫入 error_logs。**v2.14.0 起發版時自動巡檢線上錯誤日誌並產生待修復清單；v2.17.0 起巡檢完會把讀到的那批標記為已處理（紀錄不刪除、可切換檢視），首頁提示只針對下一批新錯誤。** |
| 資料完整性 | 軟刪除防誤刪機制 | 已完成 | is_deleted 欄位；管理員刪除僅標記，前端 GET 自動過濾。 |
| 資料安全 | 資料庫層級存取控制 | 已完成（有取捨） | 見下方「已知限制」：目前 SUPABASE_KEY 是 anon key，僅存在伺服器端；新資料表關閉 RLS、由後端 API 把關。建議日後改用 service_role key 並開啟 RLS + policy。 |
| 安全標頭 | CSP / X-Frame-Options / nosniff / HSTS / Referrer-Policy | 已完成 | CSP 維持 script-src 'self'、style-src 'self'、img-src 'self' data: blob:——v2.10.0 的手動海報以同源端點提供，因此沒有放寬任何 CSP 指令、也未引入任何 CDN。 |
| 前端 | 無框架、原生 JS + 本地 Tailwind | 已完成 | 不使用任何 CDN 或第三方前端套件（符合嚴格 CSP）。 |
| 安全性 | 漏洞總檢查與修復 | 已完成 (v2.12.0) | 逐項盤點 44 條路由（授權覆蓋、水平越權、mass assignment、注入、XSS、JWT、密碼儲存、檔案上傳、安全標頭），修復 7 項：錯誤日誌靜默、403 誤判為登入逾期、公開端點無節流、CSV 公式注入、錯誤訊息外洩、API 可被快取、日誌系統自身失敗不可見。完整報告：docs/安全性檢查-v2.12.0.md。 |
| 安全性 | 密碼明碼儲存（v2.12.1 後續檢查發現並修復） | 已完成 (v2.12.1) | v2.12.0 報告發布後追查日誌寫入問題時，發現早期帳號的密碼是明碼存在資料庫（8 個帳號全部）。已修：登入成功自動升級為 scrypt 雜湊、批次腳本一次處理尚未登入的舊帳號、健康端點可查尚待升級數量、雜湊實作抽為共用模組。殘留風險：明碼曾存在於資料庫與任何讀過該表的備份，系統無法得知是否已外流 → 建議提醒使用者更換在其他網站重複使用的密碼。 |
| 前端 | iOS／PWA 裝置能力判斷 | 已完成 (v2.11.0) | CMNotify.pushSupportState() 分辨 ios-needs-homescreen（iOS 需先加入主畫面）／denied（權限被拒）／unsupported／ok，UI 依結果顯示可操作指引而非一句「不支援」；iPadOS 13+ 以 maxTouchPoints 辨識（UA 會偽裝成 Mac）。 |
| 依賴 | 後端僅 web-push 為新增依賴 | 已完成 (v2.10.0) | 僅伺服器端使用，不影響 CSP。 |

## 三、資料庫 Migration（必須手動執行）

| 檔案 | 內容 | 必要性 |
|---|---|---|
| 2026-09-23-v2.7.0-competition-category-tags.sql | competitions 加入 category、tags | 已執行 ✅ |
| 2026-09-24-v2.9.0-users-registration-teams.sql | 新增 registrations、competition_teams、push_subscriptions、push_log；competitions 加入 is_team_event、team_size、registration_deadline、max_registrations；放寬 admin_users.role | 已執行 ✅（但其中的 enable row level security 必須靠下一支修正） |
| 2026-09-24-v2.10.0-poster-and-push.sql | 新增 competition_posters、app_settings；competitions 加入 poster_updated_at；關閉上述新表的 RLS 並補上 GRANT | 已執行 ✅（2026-09-24 完成；VAPID 金鑰已自動產生） |
| 2026-09-25-v2.12.0-user-management.sql | admin_users 加 is_active／last_login_at／updated_at；error_logs 加 severity／resolved／resolved_at／resolved_by | 已執行 ✅（2026-09-25） |
| 2026-09-26-v2.13.0-enable-rls.sql | 對 10 張表啟用 RLS、撤銷 anon／authenticated 權限（service_role 不受影響） | 已執行 ✅（2026-09-25；anon 全數被擋、service_role 讀寫正常） |
| 2026-09-26-v2.15.0-admin-2fa.sql | admin_users 加 totp_secret／totp_enabled／totp_confirmed_at／totp_recovery_codes／totp_last_step | **待你執行**（未執行時兩步驟驗證自動停用並提示檔案路徑，其他功能完全不受影響） |

執行方式：Supabase Dashboard → SQL Editor → 貼上整份檔案 → Run。SQL 皆為 idempotent（可重複執行）。

## 四、版本歷程

| 版本 | 內容 |
|---|---|
| v2.4.x | 分享海報、軟刪除、資源回收桶、稽核日誌 |
| v2.5.0 | 深淺色模式自動適配（prefers-color-scheme） |
| v2.6.0 | 主題三態手動切換（跟隨系統→淺色→深色）＋色票 CSS 變數重構 |
| v2.7.0 | 賽事分類與標籤系統、日曆檢視模式 |
| v2.8.0 | 訊息提醒（Web Notification）、一鍵匯入／匯出 CSV、專案清理 |
| v2.9.0 | 普通用戶註冊、線上報名（需登入）、組隊比賽、隊伍編排、報名截止與名額上限、密碼雜湊 |
| v2.10.0 | 手動上傳海報（取代自動生成）、Web Push 推播訂閱、cron 排程、RLS 修正 |
| v2.11.0 | 手機介面優化、可安裝成 App（PWA）與 iOS 推播支援、深連結直達賽事、我的報名加入行事曆（.ics／Google 日曆）、報名名單匯出 CSV、剩餘名額顯示；修正「未登入仍可下載 CSV」與「我的報名顯示 migration 訊息」 |
| v2.11.1 | 安全性修正：cron 端點在未設定 CRON_SECRET 時必須拒絕（不可被任意觸發） |
| v2.12.0 | 使用者管理（管理員以上可建立與管理帳號、網站擁有者可調整角色）、錯誤日誌修復與強化、推播發送紀錄、帳號安全強化（登入鎖定／停用帳號／最後登入）、漏洞總檢查與 7 項安全修復 |
| v2.12.1 | 密碼儲存安全強化：修正「早期帳號密碼為明碼儲存」，登入時自動升級為 scrypt 雜湊，並提供批次升級腳本與可觀測數量；雜湊實作抽為共用模組 lib/passwords.js |
| **v2.12.2** | **修正「調整他人角色時一點選單就跳確認、權限改不動」：角色下拉原本掛在 click 上，會以舊角色值觸發確認框並送出舊值；改為只處理 change，並新增「真實操作順序」迴歸測試** |
| v2.13.0 | 資料庫存取安全強化：金鑰改用 service_role（anon → service_role）、健康端點可觀測 `db_key_type`／`rls_ready`、新增 RLS 遷移檔與 ROLLBACK 語法、檢查腳本與文件工具進 repo（不再被暫存目錄清理刪除） |
| v2.14.0 | 登入鎖定改為「帳號 + IP」雙重計數（不再能靠連續失敗鎖住他人帳號）、未處理錯誤日誌主動提示（登入橫幅＋選單計數）、錯誤日誌自動巡檢（一鍵產生「待修復清單」並自動更新本文件第九節）、瀏覽器檢查工具進 repo（零外部依賴的 CDP 驅動） |
| v2.15.0 | 管理員兩步驟驗證（TOTP，手寫不引套件、RFC 官方測試向量驗證、登入兩段式、備援碼只存雜湊、防重放、暴力嘗試計入鎖定、上級可重設）、正式站「訪客視角」檢查（真實 Chrome 確認資料真的渲染，補上 HTTP 煙霧測試看不到的盲點） |
| v2.16.0 | 稽核日誌強化：依使用者／動作／時間區間／關鍵字篩選、分頁瀏覽、匯出 CSV（公式注入防護＋BOM）、保留天數清理（最少 30 天、可先預覽、預設不自動刪；設 `AUDIT_RETENTION_DAYS` 才由每日排程順便清）、每筆匯出與清理都留下稽核 |
| v2.17.0 | 每次更新巡檢後**自動把讀到的錯誤日誌標記為已處理**：新增批次標記端點（單次上限 500 筆、一定留稽核、未執行 migration 時回 503 降級）、`npm run triage` 讀取→分析→標記一次完成、錯誤日誌視窗加「🏷️ 全部標記已處理」按鈕、標記不等於刪除（可切換檢視）、首頁提示只針對下一批新錯誤 |
| **v3.6.2** | **現場報到與現場代報名（Roadmap 8.7 ⑤）**：依使用者 2026-09-26 定的方式（**先由管理員勾選簽到**，不做 QR 掃碼）。**資料庫**：`migrations/2026-09-27-v3.6.2-attendance.sql`（可重複執行）新增 `registrations.attended_at`（簽到時間，null＝未簽到）／`attended_by`（操作者）／`onsite`（現場代報名），並建 `idx_registrations_attendance (competition_id, attended_at) where is_deleted=false`；RLS 沿用既有政策。**端點**：`POST /api/registrations/:id/attendance`（管理員以上；**只有正取可簽到**，候補／待審核回 400 並說明先核准或遞補）與 `POST /api/competitions/:id/onsite-registration`（管理員以上；守名額上限、額滿回 409、同名不分大小寫不重複建立並回既有編號、姓名等於既有帳號時自動連結 `user_id`）。**列表**：`/registrations` 與 `/teams` 多回 `attendance`（應到／已簽到／現場代報名）、`attendance_schema_ready` 與每筆 `attended_at`／`attended_by`／`onsite`（統計只給管理員）。**介面**：賽事卡片→「報名／隊伍」彈窗最上方「🎫 現場報到」——可搜尋姓名、單手簽到／取消簽到、統計與「還有 N 位未簽到」提示、「➕ 現場代報名」表單（送出後自動收起）、CSV 匯出多「簽到」「現場代報名」兩欄可列印當紙本備援；資料庫沒跑 migration 時整塊不出現（後端 503 指出要跑哪一支）。**稽核**：`REGISTER_ATTENDED`／`REGISTER_ATTENDANCE_UNDONE`／`REGISTER_ONSITE`（已登記於 `AUDIT_ACTION_LABELS`，守門測試通過）。**說明書**：新增「🎫 現場報到與現場代報名」段落；管理員操作清單第 9 步由「尚未有系統功能」改為真正操作位置與限制，並在測試中禁止說明暗示有 QR 掃碼。**測試**：`npm test` **603 通過 / 0 失敗**（新增 `tests/attendance.test.js`，含 401／403、候補不可簽、取消簽到、404 邊界、同名（含大小寫）不重複、額滿擋下、連結既有帳號、統計只給管理員、未跑 migration 回 503）；瀏覽器檢查新增 `tests/browser/attendance-check.js` **35 項**（真實 Chrome 手機尺寸）；正式站煙霧新增 **8 項**唯讀斷言（`attendance_schema_ready`、統計欄位齊全、簽到數不超過應到、未登入不可簽到 401、一般角色不可簽到 403、不存在 id 404、未登入不可代報名 401、一般角色不可代報名 403）。**RED 驗證**：拿掉「只有正取可簽到」與「同名檢查」兩項規則，測試分別在對應斷言轉紅；還原後重新 grep 關鍵行並重跑綠燈。**未做**：QR 掃碼報到屬第二階段。 |
| **v3.6.1** | **錯誤日誌：截圖上限、列表瘦身、改為按下才下載（8.6 ④）**：①`POST /api/logs/error` 未登入可寫卻把 `screenshot` **整包**存進資料庫（全域 body 10mb×每分鐘 30 次＝匿名者可一次寫約 10MB；且無格式檢查）→ 在 `server.js` 全域之前為該路徑掛 **512kb** 的 JSON／urlencoded 解析器（超過 413，並由全域 Error Handler 記一筆警告級 `request_body_too_large`）。②只接受真正的 `data:image/(png|jpeg|webp|gif);base64,` 且 ≤**400,000 字元**；不合法或過大→**丟圖片但照記錯誤**（`stack_trace` 寫明原因，前端據此不顯示按鈕）。③列表改明確欄位（不含 `screenshot`）＋`has_screenshot`（由標記算出）。④新增 `GET /api/admin/error-logs/:id/screenshot`（**只有 super_admin／web_owner**；user 與 admin 皆 403），前端改按「🖼️ 檢視截圖」才下載。**實測**：同一份含 400KB 截圖的資料，列表回應 **400,974 → 936 位元組**、列表 DOM 內嵌圖片 1→0。順手：假 Supabase 新增 `select` 欄位投影（原本回傳整列、比真實 PostgREST 寬鬆，讓這類斷言驗不出來；並因此抓到「RED 還原把修正覆蓋掉」的狀態）。新增 `tests/error-log-limits.test.js` 與 `tests/browser/error-log-screenshot-check.js`（21 項，能量到列表回應實際大小）；兩項關鍵斷言皆做 RED 驗證。測試：`npm test` **602 通過 / 0 失敗**。無 DB migration／CSP／套件變更。 |
| **v3.6.0** | **使用說明（依身分給對應內容）＋ 管理員操作清單 ＋ 路由覆蓋率變成常駐檢查**：① 新增「📖 使用說明」（選單，**所有人皆可用**）：16 段說明分成四種身分層級（所有人／一般使用者／管理員／超級管理員以上），內容放在 `public/js/guide.js`（UMD，前端與 Node 測試共用），**依身分過濾在資料層就決定**（未知或未登入角色一律比照訪客；一般使用者根本拿不到管理員段落，不是畫出來再藏）；可關鍵字搜尋（找不到有明確提示）、桌機左清單右內容、手機改橫向排列不佔滿畫面。② 管理員操作清單：辦一場賽事的 12 步，每步都寫明「在系統的哪裡做」（含審核候補、隊伍、列印名單、成績草稿與公布、異常處理）；**現場報到誠實標示「尚未有系統功能，請列印名單人工勾選」**。③ 路由覆蓋率常駐化：`scripts/test-coverage.js`＋`npm run check:coverage`＋守門測試 `tests/route-coverage.test.js`，**92 條路由 92 條有測試**（RED 驗證：移開 v3.5.4 的三支新測試檔，掃描立刻列出那 8 條並非零退出）。④ 順手修掉：權限對應表少一個逗號導致 `applyMenuVisibility` 整段 `TypeError`（`node --check` 看不出來，是新瀏覽器檢查登入後炸出來的）；`tests/browser/lib/cdp.js` 新增原生對話框自動處理與記錄（過去 `alert`／`confirm` 一彈出就整頁卡住、之後每個 evaluate 只會逾時）。另外 `var(--cm-border, #e2e8f0)` 的變數名專案裡不存在（「有 fallback 就不算未定義」的規則放行了它，深色模式下會出現亮線）→ 改用 `--cm-bd-200`，並在 `scripts/class-coverage.py` 新增②-b提示把這類打錯字列出來。測試：`npm test` **601 通過 / 0 失敗**＋瀏覽器檢查 **21 支套件全綠**（新增 `guide-check.js` 38 項）。無 DB／CSP／後端 API 變更。 |
| **v3.5.4** | **選單彈窗在電腦上不再細成一條 ＋ 補齊 8 條沒有測試的端點**：① 使用者反映「右上角選單裡的按鈕點開後在電腦看上去比較細（以通知為例）」→ 實測 1920／1440 面板僅 512px、內文 12px、按鈕 24px；改為桌機面板下限 704px、內文 13px、按鈕 32px，並把平板的按鈕從 24px 提到 32px（原本比手機的 36px 還小），手機完全不變；純 CSS（依 `max-w-*` 分級加 `min-width`），另補上 6 個漏掉 `cm-modal-panel` 的彈窗。② 補齊 8 條沒有測試的端點（`POST /api/admin/login`、`PUT /api/auth/change-password`、`POST /api/logs/error`、`POST /api/push/unsubscribe`、回收筒 `trash`／`deleted`／`restore`／`hard-delete`）：3 個新測試檔（權限階梯、節流、欄位截斷、雜湊儲存、永久刪除真的刪除、稽核），並對三項關鍵斷言做 RED 驗證。新增 `tests/browser/modal-size-check.js` 納入 `check:prod`。測試：`npm test` **594 通過 / 0 失敗**＋瀏覽器檢查全綠。無 DB／CSP／後端邏輯變更。 |
| **v3.5.3** | **修正 v3.5.2 的一項斷言**：`check:prod` 第 6 節原本斷言 `self_test_skipped >= 1`，但那是**單一實例記憶體計數**，Vercel serverless 多實例下讀到 0 而誤判失敗（96/1）——功能其實有效（前後對帳 33 筆 → 33 筆）。改為純行為驗證：當場送兩個標記過的預期錯誤請求，斷言沒有新增 node/curl 來源日誌、日誌總數沒有增加；計數器降為資訊列。正式站 97 通過 / 0 失敗。另修 `ops-stats-api.test.js`：斷言拿 UTC 日期比本地日期，澳門 00:00–08:00 固定假失敗（v3.5.2 worktree 重現確認非本版造成）→ 改用與伺服器共用的 `public/js/stats.js` 算期望值並收緊為 `===`（附 RED 驗證）。單元測試 591 通過 / 0 失敗。教訓：多實例環境不能用行程內計數器證明跨請求行為；日期比較不可一邊 UTC 一邊本地。 |
| **v3.6.5** | **QR 掃碼報到（8.7⑤ 第二階段）**：選手在「我的報名」按「📷 報到碼 / QR」出示 QR（內容 `CM1:XXXXXXXX`）；管理員在「現場報到」多一列「📷 掃碼報到｜輸入 8 碼報到碼｜簽到」。**DB**：`migrations/2026-09-27-v3.6.5-checkin-code.sql` 新增 `registrations.checkin_code`（8 碼、字母表去掉 I／L／O／0／1、同賽事內唯一索引）——**不可用報名編號代替**（編號可猜，等於讓人幫別人簽到）。**端點**：`POST /api/registrations/attendance-by-code`（管理員以上；小寫／空白／連字號容錯、`CM1:` 前綴容錯；已簽到回 409 並說出誰在何時簽的；候補 400；軟刪除 404；未跑 migration 503）；`/api/my/registrations` 帶出本人報到碼（lazy 產生、撞碼重試）。**介面**：掃碼用 `BarcodeDetector`（只有 Android／桌面 Chrome 有）＋手打 8 碼；**沒有掃碼能力的裝置按下按鈕直接說明改用輸入**，不開掃不到東西的畫面。**手寫 QR 產生器** `public/js/qr.js`（無外部套件、CSP 不變）：byte mode＋等級 M＋版本 1–3（單一 RS 區塊）＋8 種遮罩取最低懲罰分＋SVG（crispEdges）。**三層驗證**：結構 → 測試內另寫規範讀取器反向讀回 → **macOS Vision 真正解碼**（`scripts/qr-decode.swift`）；**第三層抓到真 bug：格式資訊位元順序寫反**（前兩層用同一套錯誤假設而「驗過」、畫面也看不出來）。**測試**：`npm test` 610 通過 / 0 失敗（`qr.test.js` 4 段、`checkin-code.test.js` 7 段）＋瀏覽器新增 `checkin-qr-check.js` 28 項（含**模擬 iPhone** 把 `BarcodeDetector` 拿掉，驗降級路徑）；路由覆蓋 99／99。**說明書**：現場報到段落改寫成三種方式，並明講 iPhone／iPad 沒有掃碼 API（v3.6.2 曾驗「不可以提到 QR」，本版把斷言翻轉）。**未做**：iOS 不額外引入解碼函式庫（會破壞無外部套件與 CSP 原則）；報到碼沒有「換一組」功能。 |
| **v3.6.4** | **賽事公告的一鍵推播（8.8⑥ 加分項）**：v3.6.3 讓公告「不訂閱也看得到」，這版把「要推播的人一鍵就推得到」補上。**設定**：`PUSH_SETTING_LABELS.event_notice`／`PUSH_SETTINGS_DEFAULTS.push_event_notice`／`PUSH_EVENT_SETTING_KEYS.notice` 新增站台開關「賽事公告通知（取消／延期／最新消息）」（推播設定頁多一個勾選框）。**端點**：三個端點都接受 `notify: true`，透過 `notifyRegistrants()` 只推給**該賽事未刪除的報名者**（自己取消報名的人不再收到），並回傳 `push`（`sent`／`total`／`targets`／`skipped`）與人看得懂的一句話（例如「已推播給 12 位已報名者（實際送到 9 個裝置）」）。**兩層開關**：站台開關關掉時即使勾選也不送，且明確回報「未推播（站台設定已關閉…）」；**推播失敗不影響取消／延期本身**，結果記進 `push_log`（kind `notice`）。**介面**：公告彈窗上方新增勾選框（預設勾選）。**測試**：`npm test` 605 通過 / 0 失敗（`competition-notice.test.js` 新增 4 組推播斷言）；瀏覽器 23 支套件全綠（含推播設定 44 項）；線上為 v3.6.4。**未做**：QR 掃碼報到。 |
| **v3.6.3** | **賽事取消／延期＋卡片最新消息（8.8⑥）**：賽事狀態原本完全由日期推導，「延期過」「取消」算不出來，臨時變更只能靠推播（要訂閱，沒訂閱的人看不到）。**資料庫**：`migrations/2026-09-27-v3.6.3-cancel-postpone-news.sql`（可重複執行）新增 `cancelled_at`／`cancel_reason`／`postponed_date`／`postponed_time`／`news`／`news_updated_at`＋`idx_competitions_notice`。**端點**（皆限管理員以上、皆留稽核）：`POST /api/competitions/:id/cancel`（`{reason}` 取消、必填原因；`{cancelled:false}` 復原）、`POST /api/competitions/:id/postpone`（記新日期時間、**原訂時間保留**；`{postponed_date:null}` 取消標記；日期要真實存在且不可與原訂相同）、`POST /api/competitions/:id/news`（公告 300 字上限，空字串＝清空）；未跑 migration 一律 503 並指出要跑哪一支。**規則**：取消／延期由 `public/js/competition-state.js`（前後端共用唯一來源）判定並**排在日期邏輯之前**，兩者都**立刻停止報名**（避免變動期間搶名額與名額對不上帳），只填消息不影響報名。**介面**：卡片新增公告區（⛔ 取消＋原因／🕒 延期至…（原訂…）／📣 最新消息＋更新時間）與管理員按鈕「📣 公告／取消」，彈窗內可取消／復原／延期／取消延期／更新與清空消息。**說明書**：新增「📣 賽事取消、延期與最新消息」段落與管理員操作清單第 13 步。**測試**：`npm test` **605 通過 / 0 失敗**（新增 `tests/competition-notice.test.js` 10 段與 `tests/notice-render.test.js` 7 段——後者直接抽取 `app.js` 的真實渲染函式驗證輸出，**因此抓到「更新時間顯示 UTC 而非本地時間」的真實 bug**）；路由覆蓋 **99／99**；路由清單快照 **109 條**。**未做**：取消時一鍵推播公告、QR 掃碼報到。 |
| **v3.5.2** | **驗證流程不再製造假錯誤日誌（8.6 ③）**：正式站 33 筆錯誤日誌中 30 筆（91%）是自動化檢查自己造成的（29 筆 `malformed_json_body` + 2 筆 `auth_invalid_token`，UA 全是 node，`check:prod` 每跑一次多一筆）。做法：檢查流量帶 `X-CM-Self-Test`（`JWT_SECRET` 簽、`purpose:'self_test'`），伺服器驗簽通過就略過錯誤日誌、**回應行為完全不變**；沒帶／偽造／亂帶一律照記（fail-safe）；無效權杖的日誌節流不再被自檢流量佔用；`health` 新增 `self_test_skipped`；`check:prod` 新增 2 項斷言自動守門。測試：`npm test` 591 項（590 通過 / 0 失敗、1 略過）＋瀏覽器 621 項全綠。順手修掉 `competition-state-check.js` 的邊界競態（預期寫死 `603`，而種子 `606` 的報名開始是「執行當下 +30 秒」→ 跑慢就假失敗；已用 v3.5.1 worktree 重現確認與本版無關，並做 RED 驗證）。無 DB／CSP／前端變更。 |
| **v3.5.1** | **熱修（同日）**：v3.5.0 上線後對正式站實測發現**自家網域的 POST 被 CSRF 擋成 403**（登入者完全無法寫入）——根因是來源判斷比對了 scheme，而伺服器在 Vercel proxy 後面（`req.protocol` 為 `http`、未設 `trust proxy`）。改為只比對主機（接受 `X-Forwarded-Proto`）＋白名單；CORS 改自寫具名中介層（移除 `cors` 套件，非白名單完全不給 ACAO、預檢回 204）；移除 `vercel.json` 對所有路徑硬塞的 ACAO（避免與 Express 重複標頭）。測試：`npm test` 588 項（587 通過 / 0 失敗、1 略過）＋瀏覽器 621 項＋新增 2 項代理形狀／預檢迴歸測試。 |
| **v3.5.0** | **P0 安全修復：CORS 白名單＋登入憑證改 HttpOnly Cookie（ZAP 2 個中風險）**：CORS 由裸設定（回 `Access-Control-Allow-Origin: *`）改為只反射自家來源與 `CORS_ALLOWED_ORIGINS` 白名單；登入／2FA／註冊改以 `Set-Cookie: cm_token`（`HttpOnly`／`SameSite=Strict`／`Secure`／`Max-Age=43200`）發憑證、**回應不再回傳 token**、前端不再寫入 `localStorage`、登入狀態改問 `GET /api/auth/me`、登出請伺服器清 cookie；新增 `csrfCookieGuard`（帶 cookie 的寫入操作來源不符 → 403）；`Authorization: Bearer` 保留給腳本與 Cron（cookie 優先）。測試：`npm test` 586 項（585 通過 / 0 失敗、1 略過）＋瀏覽器 621 項＋新增端到端安全性質檢查 3 項（localStorage／`document.cookie` 都讀不到憑證）。無資料庫／CSP 變更。 |
| **v3.4.0** | **`server.js` 拆模組（P4，重構不變行為）**：6,979 行 → **2,551 行**，88 條路由拆成 `routes/` 下 **19 個模組**，以 `require('./routes/x')(app, ctx)` 依原本順序掛載；共用可變狀態集中為 `serverState`；新增 **路由清單守門測試**（93 條路由＋7 層中間件的數量與順序快照，順序錯了不會報錯、只有它抓得到）；順手修掉兩個與時間有關的既有測試缺陷。無資料庫／CSP／前端變更。測試：`npm test` 581 通過 / 0 失敗（1 略過）＋瀏覽器 618 項＋正式站 95＋8＋15。 |
| **v3.3.0** | **賽事規程 PDF 附件（P1-6）**：新增 `competition_docs` 表（一場一份）＋公開讀取／管理員上傳／管理員移除三個端點；卡片「📘 規程」按鈕（另開新視窗、可另存）、編輯表單可上傳／更換／移除；檔案把關＝**看檔頭 `%PDF-`**（不看副檔名／MIME）＋上限 3MB（Vercel 4.5MB 請求上限與 base64 膨脹）；列表只帶中介資料不撈檔案內容。測試：單元 18 項＋真 Chrome 32 項（含假 PDF 被後端擋下、訪客位元組相同、大檔在畫面就被擋）＋正式站煙霧 4 項。 |
| **v3.2.1** | **版面空間優化**：桌機卡片內容欄原本被右側按鈕區擠到只剩 84px（7%）、資訊一行一行直排、大螢幕主容器固定 1280px。修正：按鈕區限寬 `min(30rem, 44%)`、資訊列改自動欄數格狀、內容欄可壓縮、大螢幕容器放到 1560px。結果：桌機內容欄 84→660px、卡片高 612→219px；手機維持兩欄且 13 顆按鈕全可見。新增 `tests/browser/layout-check.js`（37 項，四種尺寸），並實測拿掉修正會亮紅。 |
| **v3.2.0** | **名單列印版（P1-7）**：新增列印預覽視窗（`public/js/printdoc.js` 規則 UMD＋`public/css/print.css`）；名單只印已錄取、候補另段標示順位、可切換含候補／含待審核／簽到欄／橫向；成績表名次照原值；`@page A4` 且列印時只留那張紙（表頭每頁重複、單列不跨頁）；不連外、不含個資。測試：單元 16 項＋真 Chrome 32 項（含用 CDP 模擬列印媒介實測介面被隱藏、`Page.printToPDF` 真的產出 PDF 但不落地）。 |
| **v3.1.2** | **兩個修正**：①營運儀表板「推播區塊讀不到」——統計查詢讀 `push_log` 用了不存在的 `created_at`（該表是 `sent_at`，而那欄位根本沒被用到）→ 改為只取計數欄位並用 `sent_at` 排序；測試替身同步改成與真實欄位一致，並在單元與正式站煙霧加上「不得有任何讀不到的區塊」斷言。②刪除帳號送出兩次請求——`#adminList` 被綁了兩個 click 處理器（舊版遺留＋統一處理器）→ 移除舊的、加防連點守衛、順手修掉 `openAdminModal` 每次開啟都重綁重整按鈕；新增真 Chrome 檢查直接數請求數與稽核筆數（修好前實測 2 個請求／2 次確認／2 筆稽核）。 |
| **v3.1.1** | **修正 v3.0.0 migration 無法執行**：`2026-09-27-v3.0.0-perf.sql` 對 `push_log` 建索引時用了不存在的 `created_at`（該表時間欄位是 `sent_at`），PostgreSQL 回 `42703`，而 SQL Editor 是整份腳本一個交易 → 一句錯就全部回滾，連沒問題的縮圖欄位也沒建立。已改為 `sent_at` 並把該檔 13 條索引、3 個欄位逐條對照正式資料庫驗證；修正版已實際套用並讀回確認（縮圖欄位、10 條索引、成績表與其 RLS、公布欄位全部到位），正式站成績端點由 503 恢復為 HTTP 200。 |
| **v3.1.0** | **成績與結果（P1-3）**：補上「賽後」那一段。`competition_results`（一筆報名一筆成績，`rank` 刻意不設唯一鍵——並列是常態）＋ `competitions.result_published_at`（前台看不看得到的唯一依據）；管理員「🏆 成績登錄」（已核准名單、自動排名、草稿、公布、取消公布）＋參賽者「🏆 成績」成績表與「我的報名」顯示名次；`GET /api/competitions/:id/results`、`…/result-sheet`、`PUT …/results`、`POST …/results/publish|unpublish`、`GET /api/my/results`；新推播事件「賽事成績公布通知」（預設開啟，只推給有報名的人）；**未公布時連統計都不回**；儲存前整批驗證（名次／歸屬／未核准／重複）失敗就一筆都不寫；稽核 `SAVE_RESULTS`／`PUBLISH_RESULTS`／`UNPUBLISH_RESULTS`。 |
| **v3.0.0** | **後台營運儀表板與效能（統計圖表＋分頁優化＋海報縮圖）**：營運儀表板（六個關鍵數字＋三張手寫 SVG 圖＋效能現況，可切 7／14／30 天；只回聚合數字、缺表降級、快取 60 秒）；列表與報名名單的 opt-in 分頁（沒帶 limit 行為不變、`paged_by` 標示 db／memory、單次上限與壞參數保護）；分頁時只撈本頁賽事的報名（成本 O(全部) → O(本頁)）＋查詢次數守門測試；海報縮圖（瀏覽器 canvas 產生、`?variant=thumb` 沒有縮圖自動退回原圖、卡片延遲載入且視窗外不下載）；migration `2026-09-27-v3.0.0-perf.sql`（縮圖欄位＋索引）。修掉開發中發現的三個問題：伺服器端雙重切片（帶 offset 會少一頁）、換海報沒清舊縮圖、測試圖片無法解碼。 |
| v2.27.1 | **把「按鈕隱形」納入自動檢查（只加檢查與文件，未動功能）**：使用者回報公告中心的「發布公告」按鈕同樣看不到，與推播設定「儲存設定」同一根因（`bg-sky-600` 色系不存在→白字配透明底；v2.27.0 已改成 `blue-*`）。新增 `tests/browser/live-style-check.js`（已納入 `npm run check:prod`）：對正式站以真實 Chrome 算過 CSS 後檢查四顆送出按鈕有底色且字色≠底色、兩顆連結式按鈕不是白字、並全頁掃描「字色＝底色」的隱形文字；公告中心介面檢查 +4 項、工作人員介面檢查 +1 項按鈕可見性斷言。線上實測 8 項全綠（確認公告中心按鈕已修好）。 |
| v2.27.0 | **場地地圖連結＋工作人員指派（P1-5）＋修復推播設定確認按鈕**：**地圖連結**——`competitions.map_url`＋卡片「🗺️ 地圖」連結（沒填＝用地址自動產生 Google 地圖搜尋連結，舊賽事免補資料；只接受 http/https，不安全或不像是網址的一律 400 說明）；**工作人員指派**——`competition_staff`（同一場同一人一列，角色裁判／記錄／攝影／醫護／其他、備註、指派者）＋`GET /api/competitions/:id/staff`（公開、只回角色與帳號名稱）、`POST /api/competitions/:id/staff`（管理員以上，指派／改角色）、`DELETE /api/staff/:id`（移除）；`/api/registration-counts` 多回 `staff` 聚合人數；規則唯一真實來源 `public/js/venue.js` 與 `public/js/staff.js`（UMD 前後端共用）。**修掉「推播設定的確認按鈕看不見」**：`bg-sky-600` 這個色系在 Tailwind 靜態檔與 custom.css 都不存在（白字配透明底＝隱形），26 處 `sky-*` 改用專案既有的 `blue-*`，補上 `hover:text-amber-900` 規則，並把顏色 class 覆蓋率檢查 `scripts/class-coverage.py` 搬進 repo（色系白名單補 `sky`、`accent-*` 納入掃描）＋新增守門測試 `tests/class-coverage.test.js` 進 `npm test`。**修正資料表探測快取**：負結果改為只快取 60 秒，migration 執行後暖實例不再一直回報「尚未啟用」（線上實際發生過）。migration `2026-09-26-v2.27.0-venue-staff.sql`（未執行時地圖仍可用、賽事照常建立但回 `map_url_saved:false`、工作人員端點降級）。 |
| v2.26.0 | **推播失敗可視與重送＋站內公告／訊息中心（P2 收尾）**：**推播失敗可視與重送**——`push_log` 補上失敗筆數／原因／內容／對象／重送欄位（`failed_count`／`error_detail`／`payload`／`target_user_id`／`target_user_ids`／`resend_count`／`resend_at`／`resend_sent_count`），`GET /api/admin/push-logs` 多回 `detail_ready`，新增 `POST /api/admin/push-logs/:id/resend`（只送原本對象、手動不受自動開關影響、寫回重送紀錄、留稽核 `RESEND_PUSH`、`404／410` 順手停用訂閱、舊紀錄無內容回 400）；每日摘要與公告推播的失敗也一起記下明細。**站內公告／訊息中心**——`GET /api/my/announcements`（依對象與時間回可見公告＋未讀數＋我的訂閱分類）、`POST /api/my/announcements/read-all`（冪等 upsert）、`POST /api/my/announce-categories`、管理端 `GET/POST /api/admin/announcements`、`PATCH /api/admin/announcements/:id`（變更摘要＋稽核 `CREATE_ANNOUNCEMENT`／`UPDATE_ANNOUNCEMENT`）；可見性規則唯一真實來源 `public/js/announcements.js`（UMD 前後端共用）；發布可同時推播（只推給對象使用者，站台開關 `push_event_announce` 可關）；前端「📣 公告中心」含未讀徽章、訂閱分類、管理公告區、`?announce=<id>` 直達。migration `2026-09-26-v2.26.0-push-log-detail.sql`、`2026-09-26-v2.26.0-announcements.sql`（未執行時各自降級：紀錄照常看但無明細／公告中心顯示尚未啟用，其他功能不受影響）。**順手修掉**：公告管理清單下架／重新上架方向相反（真 Chrome 檢查抓到）。 |
| v2.25.0 | **推播時間與事件可設定（P2）＋修好頁面上方的版本號**：導覽選單「⏰ 推播設定」（`GET/POST /api/push/settings`，存在既有 `app_settings`、**不需 migration**）——每日摘要總開關、發送時間（澳門時間，預設 09:00）、摘要含哪些事件（新發布賽事／即將開賽提醒）、即時通知事件（報名審核結果／候補遞補）各自開關，儲存回報每項前後值並留稽核 `UPDATE_PUSH_SETTINGS`；每日排程（`/api/cron/reminders`，Vercel 每天 01:00／13:00 UTC）**一天只發一次、錯過在稍晚那次補發**（記 `push_last_digest_date`），沒訂閱者／沒到期賽事／事件全關都明確回報 `digest.reason`；被關掉的即時通知**連推播訂閱都不查**，**讀不到設定一律視為開啟**（降級回 `schema_ready:false`＋說明），推播失敗不讓排程倒下（稽核清理與週期賽事照做）。**頁面上方版本號改由 `GET /api/version` 帶入**（不再寫死在 HTML），新增 `tests/version-consistency.test.js` 守門（package.json＝`<title>`、不得出現寫死的 `(vX.Y.Z)`、副標由 API 更新）。 |
| v2.24.0 | **賽事複製與週期性（P1-4）**：**一鍵複製賽事**（`POST /api/competitions/:id/duplicate`；照抄設定、不搬報名／隊伍／海報，名稱加「（複製）」且不疊加，複製品不帶週期，稽核 `DUPLICATE_COMPETITION`）；**週期性賽事**（`POST /api/competitions/:id/recurrence` 設定每週／每兩週／每月＋結束日、`POST .../recurrence/next` 立即建立下一場；每日 cron `/api/cron/reminders` 順便處理，兩者共用 `CMCompetitionState.planRecurringCreation()` 同一份純函式：每月遇缺日夾月底、只提前 30 天內建立、**一次只排一場**、報名時間一起往後挪、超過結束日就停、落後回報不亂補；週期屬整個系列，改一場同步其他場次）。migration `2026-09-26-v2.24.0-recurrence.sql`（`recurrence`／`recurrence_until`／`recurrence_parent_id`＋部分索引）。 |
| v2.23.0 | **候補收尾（P1-2 第四輪）**：**多選一次搬多筆**（候補列加勾選框，批次列顯示「已選 N 筆」＋「搬到第 N 位」，搬完相對順序照名單原順序、勾選自動清空；一次搬 N 筆時目標位置只到「人數 − N + 1」；位置換算走共用純函式 `CMCompetitionState.moveGroup()`，送出時仍走 v2.21.0 的完整順序驗證，不需要新端點）；**異動紀錄往回翻**（`GET /api/competitions/:id/waitlist/history?limit=30&offset=30` 新增 `offset`／`has_more`／`capped`，面板加「⬇️ 載入更早」與「已顯示 N 筆／已顯示全部 N 筆／達 500 筆上限」說明，單場最多往回抓 500 筆，抓取窗口＝本頁結尾 + 1 以判斷還有沒有更早的，分頁不重複也不漏）。無新 migration。 |
| v2.22.0 | **候補再補強（P1-2 第三輪）**：候補列加**拖曳把手 ⠿（pointer 事件拖拉排序，手機也能用；拖動只動畫面、放手才送 API）**與**「第 N 位」下拉（一次搬到任意順位，鍵盤也能操作）**；新增**「🕘 順位異動」面板**（`GET /api/competitions/:id/waitlist/history?limit=30`，讀稽核日誌：調整順位／手動遞補／自動遞補／審核／通知設定／取消報名，含操作者與內容，不必另開表）；新增**每賽事的「遞補通知」開關**（`POST /api/competitions/:id/waitlist/notify`，關掉時**遞補照常生效但完全不碰推播**——連訂閱都不查，三條遞補路徑都遵守，稽核記「依賽事設定未通知」）；匯出報名名單 CSV 多了「狀態」「候補順位」兩欄。**順手修掉一個老 bug**：`CMCSV.stringify()` 原本只認陣列列，傳物件列會產生「下載成功但內容全是空行」的 CSV（報名名單匯出就是這樣壞的）→ 現在物件列會自動產生表頭＋資料，並補上單元測試與瀏覽器端「攔下載驗內容」的檢查。migration `2026-09-26-v2.22.0-waitlist-notify.sql`（未執行時一律視為通知開啟，既有行為不變）。 |
| v2.21.0 | **指定遞補與候補順位調整（P1-2 補強）**：管理員可指定候補名單裡任何一位遞補（`POST /api/registrations/:id/promote`，跳過前面的人、被跳過者不受影響；與「遞補下一位」共用同一份名額守門 `planPromotion()`）；候補順位可手動調整（`POST /api/competitions/:id/waitlist/reorder`，寫入 `registrations.waitlist_order`，名單不一致整批拒絕且不改資料）；排序規則：手動數字優先→部分手動時排過的在前面→其餘照報名時間，**新報名排最後**；介面候補列加 `↑`／`↓` 與「⬆️ 遞補」、顯示順位來源、名額已滿時停用；「我的報名」順位同步。migration `2026-09-26-v2.21.0-waitlist-order.sql`（未執行時：調整順位 503＋指引、順位退回自動排序、**指定遞補仍可用**）。 |
| v2.20.0 | **報名審核與候補名單（P1-2）**：賽事兩個開關「報名需審核」（`requires_approval`）與「額滿開放候補」（`waitlist_enabled`）；**佔名額＝已核准＋待審核、候補不佔名額**；額滿且開放候補→仍可報名並回報候補順位，沒開候補→400；管理員可核准／拒絕（含原因、審核人、通知、稽核）；**讓出名額時第一位候補自動遞補＋推播通知**（需審核的賽事遞補後仍待審核）＋手動「遞補下一位」；報名名單與統計以佔名額為準並附候補順位；待審核／候補者不可編入隊伍；我的報名顯示狀態與候補順位。migration `2026-09-26-v2.20.0-registration-review.sql`（可重複執行），未執行時整組優雅停用（端點 503＋介面誠實說明）。 |
| v2.19.0 | **賽事狀態機（含自動切換）**：狀態由後端即時判定（報名中／尚未開放報名／報名已截止／進行中／已結束／日期未定／已刪除），`/api/competitions` 每筆附 `state`／`state_label`／`can_register`／`registration_reason`／`is_full` 並支援 `?state=` 篩選；前後端共用 `public/js/competition-state.js`（唯一真實來源）；介面加**狀態篩選列**、鎖住的報名按鈕寫出原因、表單**即時狀態預覽**；表單新增「報名開始／截止」（到分鐘）並同步舊欄位 `registration_deadline`；單日賽事結束時間修正為當天 23:59:59。**稽核補齊**：`POST /api/auth/logout`（LOGOUT）、實際發送推播（SEND_PUSH）、動作標籤與實寫動作對齊＋守門測試。順修 `custom.css` 缺 11 個 class 規則與 `class-coverage.py` 的 fallback 誤判。 |
| v2.18.0 | **資料備份與還原**：全表 JSON 匯出（含 meta、每表筆數、sha256 checksum、可選含日誌／海報）、介面「💾 備份與還原」下載與還原、`npm run backup`／`npm run restore`（本機保留最近 20 份自動清理）、還原強制先檢查（dry-run）且要明確 confirm、upsert 語意**不刪除**任何既有資料、checksum 不符預設拒絕、部分失敗誠實回報、備份檔已加入 .gitignore（含帳號雜湊與個資） |
| **v2.15.0** | **管理員兩步驟驗證（TOTP，手寫不引套件）：登入分兩段、同一組碼不可重放、8 組一次性備援碼（只存雜湊）、停用需密碼＋驗證碼、上級可重設下級 2FA；另加上「訪客視角」正式站瀏覽器檢查與未執行 migration 時的自動停用** |

## 五、測試與驗證方式

| 類型 | 內容 |
|---|---|
| 單元測試 | npm test：分類／標籤正規化、CSV 解析、報名規則、海報格式、推播候選條件等純函式。 |
| API 端到端測試 | 內建「假 Supabase（PostgREST）」服務，以真實 Express app + 真實 JWT 跑授權、驗證、去重、稽核——完全不碰正式資料庫。 |
| Migration 未執行測試 | 獨立測試檔驗證「沒有新欄位時舊功能不受影響、新端點明確回 503」。 |
| 瀏覽器實測 | **已進 repo**：`npm run check:browser`（自寫 CDP 驅動，零外部套件）＝錯誤提示橫幅 13 項＋兩步驟驗證全流程 15 項，皆為真實 Chrome 手機尺寸；另 `npm run check:prod` 打正式站（HTTP 煙霧測試＋訪客視角）。 |
| 回歸測試 | 每次發版都重跑 v2.7.0～v2.13.0 的瀏覽器實測，確認舊功能未被破壞；另有「錯誤日誌巡檢」自動把線上真實錯誤轉成待修復清單並在分析後標記已處理。 |
| 線上驗證 | 部署後比對線上與本地檔案雜湊、檢查 CSP 與安全標頭、檢查新端點的降級行為。 |
| 手機尺寸實測 | headless Chrome 以 390×844（iPhone 14）與 360×640（iPhone SE） 驗證：橫向溢出、觸控目標高度、彈窗是否超出視窗、輸入框字級、篩選列排版、深色模式。 |
| 目前規模 | npm test **610 項（610 通過 / 0 失敗、0 略過）**（另有 1 項「摘要發送時間跨午夜」的情境在深夜執行時會明確略過並說明原因；v3.5.0 新增 `tests/auth-cookie-cors.test.js` 4 項釘住 cookie／CSRF／CORS 安全性質）；瀏覽器檢查 **752 項全綠**（**23 支套件**，v3.6.0 新增 `guide-check.js`、v3.6.1 新增 `error-log-screenshot-check.js` 21 項、v3.6.2 新增 `attendance-check.js` 35 項；另 `npm run check:coverage` 路由覆蓋 **99／99**）（`npm run check:browser`：提示橫幅 13＋兩步驟驗證 18（含 v3.5.0 新增「登入後 localStorage 與 document.cookie 都讀不到憑證」等 3 項）＋稽核日誌介面 22＋錯誤日誌標記 13＋備份與還原 22＋賽事狀態機 30＋報名審核與候補 49＋候補順位與指定遞補 32＋候補再補強 30＋候補收尾 23＋賽事複製與週期性 34＋推播設定 44＋公告中心與推播重送 50＋場地與工作人員 40＋營運儀表板與列表縮圖 42＋成績與結果 38＋帳號管理 20＋名單與成績列印版 32＋版面空間 37＋賽事規程附件 32＋**使用說明 38**＋**錯誤日誌截圖 21**＋**現場報到與代報名 35**＋v3.6.3 卡片公告渲染（單元層），皆為真實 Chrome 手機尺寸）；正式站 `npm run check:prod`＝HTTP 煙霧測試 **109 項**（v3.6.1 新增截圖相關 4 項、v3.6.2 新增現場報到 8 項）＋訪客視角檢查 **12 項**（v3.6.0 新增使用說明 3 項）＋線上樣式檢查 **15 項**＋彈窗尺寸量測 **41 項**（含 414px 與 1920px 的真實版面量測）（真實 Chrome：賽事卡片是否真的渲染、前端例外、手機橫向溢出、版本字串）；v2.17.0 143 項、v2.16.0 130 項、v2.15.0 118 項、v2.11.0 41 項、v2.10.0 29 項、v2.9.0 50 項、v2.8.0 50 項、v2.7.0 48 項，全部 0 失敗。拆模組後另加**路由清單守門測試**（`tests/route-inventory.test.js`＋`tests/fixtures/route-inventory.json`：**110 條**（99 條路由＋11 層中間件）的數量與順序快照（v3.6.3 新增取消／延期／最新消息 3 條端點、v3.6.5 新增掃碼報到 1 條端點），`node scripts/route-inventory.js --write` 才更新）。 |

## 六、已知限制（誠實說明）

- ~~CRON_SECRET 尚未設定~~ **已設定並驗證**（2026-09-25 實測：未帶權杖回 401、不再是 503），每日自動推播已啟用（Vercel Cron 每日 01:00 UTC ＝ 澳門時間 09:00）。

- 資料庫金鑰等級：**已改用 service_role key 且 RLS 已開啟**（2026-09-25 驗證）：anon key 對全部 10 張表回 `42501 permission denied`（含零寫入探測），service_role 讀寫正常，前台與後台功能實測無異常。金鑰只存在伺服器端，前端從不直連資料庫。
- 錯誤日誌巡檢：**每次更新時順便執行**（`npm run triage`＝讀取＋分析＋標記已處理），不使用每日自動排程（使用者 2026-09-25 決定）。
- 為什麼巡檢要順手標記已處理：發版時那批紀錄已經被分析、該修的也併進本版，留在「未處理」只會讓清單膨脹、首頁橫幅長亮，**新的錯誤反而被舊雜訊蓋掉**；標記後下一批新錯誤仍會照常提示。

- 推播的前提：需要 HTTPS（Vercel 已提供）與使用者允許通知權限；iOS Safari 必須先「加入主畫面」再從主畫面開啟才能接收推播（iOS 16.4+），v2.11.0 已在通知設定視窗提供逐步指引。

- 推播檢查頻率：伺服器端為 Vercel Cron 每日一次（Hobby 方案限制）；開賽前 24 小時的提醒因此以「每日」為粒度。網頁開啟時的本機檢查仍為每 5 分鐘一次。

- ~~資料庫尚未執行 v2.12.0 migration~~ 已執行（v2.12.1 起伺服器回報 severity／resolved 均存在），「停用帳號」「最後登入時間」「日誌標記已處理」全部可用。

- 節流與登入鎖定是記憶體計數：部署在 Vercel serverless，多實例時各自計算、重啟歸零。足以阻擋單一來源的腳本濫用，但無法阻擋分散式攻擊。

- 公開的隊伍名單：GET /api/competitions/:id/teams 讓任何登入者看到該場隊伍與成員帳號（刻意設計，方便參賽者確認隊伍）；若需更嚴謹可限制為「該場參賽者」。

- 部分非帳號端點仍回傳原始錯誤訊息：賽事／報名／隊伍的 500 回應（需登入或管理員權限），優先度較低；細節已完整寫入錯誤日誌。

- 電子郵件通知：**決定不做**（2026-09-25）；通知走站內訊息中心與 Web Push。

- Realtime 即時同步尚未實作：與「前端不直接連資料庫」的現行架構衝突。

- 自動化爬蟲尚未實作。

## 七、建議下一步（依優先順序）

| 優先 | 項目 | 說明 |
|---|---|---|
| 1 | ~~在 Vercel 設定 CRON_SECRET~~ | **已完成**（2026-09-25 驗證：端點回 401 而非 503，自動推播已啟用）。 |
| 1b | ~~執行 2026-09-25-v2.12.0-user-management.sql~~ | 已完成（2026-09-25）。 |
| 1c | 提醒使用者更換在其他網站重複使用的密碼 | 密碼曾以明碼儲存於資料庫，系統無法得知是否已外流；已全面升級為雜湊，但若同一組密碼也用於其他網站，仍建議更換。 |
| 2 | 在實機上驗證 iOS 推播 | iPhone 用 Safari 開啟 → 分享 → 加入主畫面 → 從主畫面開啟 → 通知設定 → 開啟推播訂閱（目前資料庫已有 2 筆推播訂閱）。 |
| 3 | ~~電子郵件通知 / 訂閱賽事~~ | **不做**（使用者 2026-09-25 決定）：通知改以站內訊息中心（Roadmap P2）與現有 Web Push 為主。 |
| 4 | 過期賽事自動歸檔 | 已有 cron 排程，可定期把 date 過久的賽事標記歸檔或清理 push_log。 |
| 5 | ~~開啟 RLS 並驗證~~ | **2026-09-25 已完成**：你執行遷移檔後，實測 anon key 全數被擋（10/10 表，`42501`）、service_role 讀寫正常、前台訪客與後台功能正常。 |
| 6 | 自動化爬蟲接入外部賽事 | 需先確認目標網站條款與格式。 |
| 7 | 賽事即時同步（Realtime 或輪詢） | 目前以手動重新載入為主；可先做輕量輪詢。 |

## 八、未來更新方向（Roadmap）

> 本節是滾動式規劃：每次發版後我會更新勾選狀態、補上新發現，並把「你已確認要做」的項目往前排。
> **工作節奏（新的規則）**：每完成一版，我都會停下來等你檢閱，你說「好」或給進一步指令後才繼續下一版。

### 8.1 每版的工作流程

| 步驟 | 內容 |
|---|---|
| 1 | **先跑 `npm run triage`（錯誤日誌巡檢）**：讀取線上錯誤日誌 → 更新第九節 → **把剛讀到的那批未處理紀錄標記為已處理**（紀錄不刪除，只是不再計入「未處理」與首頁提示），抓到的真問題直接併入本版修掉。只想看報告不動資料用 `npm run triage:report` |
| 2 | 實作 → `npm test` → 手機尺寸真瀏覽器檢查（`npm run check:browser`）→ 部署 → 發 GitHub Release |
| 3 | 發佈後跑 `npm run check:prod`（正式站 HTTP 煙霧測試 ＋ 訪客視角真瀏覽器檢查） |
| 4 | 更新本文件（功能總覽、版本歷程、本節勾選狀態與新發現）並同步 Google Doc |
| 5 | **停下來**：回報完成內容與驗證數據，等你檢閱 |
| 6 | 你回「好」或給新指令後，才開始下一個項目 |

> 使用者 2026-09-25 指示：**取消每日自動排程的巡檢，改成每次更新時順便檢查**（即上表第 1 步）。

工作量定義：**小**＝單一版本可完成；**中**＝可能拆 2～3 版；**大**＝需拆成多版逐步進行。

### 8.2 P0 — 安全與基礎（建議優先）

| 狀態 | 項目 | 為什麼要做 | 具體做法 | 需要你提供 | 工作量 |
|---|---|---|---|---|---|
| ✅ | 資料庫金鑰升級（anon → service_role）並開啟 RLS | 原本 anon key 搭配「RLS 關閉」是最大結構性風險：任何拿到金鑰的人可讀寫全部資料表（含帳號雜湊） | **已完成**：v2.13.0 程式支援 service_role；2026-09-25 你執行遷移檔後，實測 anon key 對 10 張表全部被擋（`42501`，含零寫入探測），service_role 的讀／寫都正常（實際寫入一筆日誌、讀回、再還原），前台訪客實測正常 | 已提供 ✅ | 完成 |
| ✅ | 登入鎖定改為「帳號 + IP」雙重計數 | 目前以帳號為鍵，別人可惡意連續失敗來鎖住你的帳號 | **v2.14.0 完成**：單一 IP 連續失敗 10 次才擋該 IP；同一帳號需「失敗 10 次且來自 2 個以上 IP」才鎖帳號（多來源攻擊）；成功登入即清除兩者 | 無 | 小 |
| ✅ | 管理員以上兩步驟驗證（TOTP） | 管理員權限可改帳號、刪資料，只靠密碼仍嫌薄弱 | **v2.15.0 完成**：`lib/totp.js` 手寫（Node 內建 crypto、RFC 4238/6238 向量驗證）、登入兩段式（中間權杖不可當憑證）、同一步不可重放、8 組備援碼只存雜湊、停用需密碼＋驗證碼、上級可重設下級 2FA。**待你在 Supabase 執行 `migrations/2026-09-26-v2.15.0-admin-2fa.sql` 後綁定手機** | 你的手機（Google Authenticator 等） | 完成（待你綁定） |
| ☑ | 稽核日誌介面強化 | 稽核紀錄已寫入但只能瀏覽，事後追查不方便 | **v2.16.0 完成**：使用者／動作／時間區間／關鍵字篩選、分頁（每頁 100 筆）、匯出 CSV（含公式注入防護與 BOM）、保留天數清理（最少 30 天、可先預覽、預設不自動刪；設 `AUDIT_RETENTION_DAYS` 才由每日排程順便清） | 無 | 小 |
| ✅ | 錯誤日誌主動告警 | 目前要主動打開後台才看到 | **v2.14.0 完成**：超級管理員以上登入即顯示提示橫幅（未處理錯誤數、近 24 小時新增、最新時間）、導覽選單顯示計數、可一鍵開啟日誌或「稍後再看」（有新錯誤才再提示） | 無 | 小 |
| ✅ | 錯誤日誌自動巡檢 → 自動產生待修復清單 → **分析後自動標記已處理** | 錯誤發生後要人工翻日誌才知道，也不會自動進入規劃；讀完沒有收尾，清單會一直累積舊訊息 | **v2.14.0 完成**讀取／分類／產生待修復清單並自動更新本文件第九節與 `docs/錯誤日誌分析.md`；**v2.17.0 完成**巡檢後自動標記已處理（批次端點、單次上限 500 筆、留稽核、可 `--no-resolve`） | 無 | 小 |
| ☑ | **CORS 改為白名單（現在回 `Access-Control-Allow-Origin: *`）** | **2026-09-26 ZAP 被動掃描回報「跨域配置錯誤」（中風險，5 處）**；根因是 `server.js:31` 的裸設定 `app.use('/api', cors())`。實測（帶 `Origin: https://evil.example` 打正式站）：`/api/competitions` 與 `/api/my/registrations` 都回 `ACAO: *` | 改成只允許自家來源（`SITE_URL`／可選 `ALLOWED_ORIGINS`；同源前端不需要 CORS header，所以沒有相容性風險），非白名單不回 ACAO；加守門測試（偽造 Origin 不回 `*`、自家不變）。**影響判讀**：我們用 `Authorization` 標頭而非 cookie，第三方沒有權杖 → 只能讀到本來就公開的資料，實際風險低，但屬明確的過寬設定。**v3.5.0 完成**：改為只反射自家來源與 `CORS_ALLOWED_ORIGINS` 白名單（帶 `Allow-Credentials`），其餘來源完全沒有 ACAO 標頭；`tests/auth-cookie-cors.test.js` 守門（偽造 Origin 不回 ACAO、白名單與同源才反射）。**v3.5.1 熱修**：來源比對不再看 scheme（Vercel 反代下 `req.protocol` 是 `http`，會把自家 https 請求誤判成跨站 → 同源 POST 被 403），並移除 `vercel.json` 平台層對所有路徑硬塞的 ACAO（重複標頭） | 無 | 小 |
| ☑ | JWT 存放位置（localStorage vs httpOnly cookie） | ZAP 中風險：`auth_token` 存在 localStorage（XSS 一旦成立就能取走；本專案 CSP 為 `script-src 'self'`、無 inline script，攻擊面小但非零）；目前權杖效期 12 小時 | 三個選項任選：(A) 接受並在文件記錄風險（成本 0）(B) **縮短效期**（例如 2 小時＋自動續期）(C) 改 httpOnly cookie ＋ CSRF token（最安全、改動最大：登入／登出／2FA／所有 fetch 都要調整）。**你 2026-09-26 選 C，v3.5.0 完成**：`Set-Cookie: cm_token`（`HttpOnly`／`SameSite=Strict`／`Secure`／`Max-Age=43200`）、**回應不再回傳 token**、前端不再寫入 `localStorage`、`customFetch` 不再手動帶 Authorization、登入狀態改問 `GET /api/auth/me`；`Authorization: Bearer` 保留給腳本與 Cron（cookie 優先）；另加 `csrfCookieGuard` 中介層（帶 cookie 的寫入操作必須來自自家／白名單來源，否則 403） | **已決定：C（v3.5.0）** | 小～中 |
| ☑ | ZAP 掃描報告的處置（含真實憑證） | **這份報告內含你的登入請求（明碼密碼）與一枚 `web_owner` 權杖（12 小時效期）＋ 2FA 完成後的權杖** | **不要提交進 repo、不要外傳**；若檔案曾離開你的電腦：改密碼（永久有效）＋必要時換 `JWT_SECRET`（可讓所有既有權杖立即失效，代價是所有人重新登入）。另外下次掃描請把範圍限制在自家網域（本次含 `update.googleapis.com`，那是 Chrome 更新服務、不是我們的，掃第三方服務有授權問題） | **你 2026-09-26 決定：報告已自行刪除、`JWT_SECRET` 不換**（那枚權杖到期即失效）；`.gitignore` 已加 ZAP 規則避免誤提交 | 已決定（不換） | 小 |
| ☑ | 資料備份與還原 | 誤刪賽事或報名資料目前無法回復 | **v2.18.0 完成**：`GET /api/admin/backup` 匯出全部核心表 JSON（含 meta、每表筆數、sha256 checksum）、介面「💾 備份與還原」可下載與還原、`npm run backup`／`npm run restore` 指令（本機保留最近 20 份、自動清理）、還原要先 dry-run 檢查並帶 confirm、採 upsert 且**不刪除**任何資料、未執行 migration 或缺表會誠實回報。**未啟用自動排程**（需要時可用 Hermes 排程＋上傳 Google Drive，見「建議下一步」） | 無 | 中 |

### 8.3 P1 — 賽事與報名核心（系統價值的延伸）

| 狀態 | 項目 | 為什麼要做 | 具體做法 | 需要你提供 | 工作量 |
|---|---|---|---|---|---|
| ☑ | 賽事狀態機（報名中 → 報名已截止 → 進行中 → 已結束） | 目前狀態全靠日期推算，無法表達「報名延長」「臨時取消」 | **v2.19.0 完成**：狀態由後端依當下時間即時判定並附在 `/api/competitions`（含 `can_register` 與原因），前後端共用同一份規則；管理員可用「開放報名中」開關與報名開始／截止時間手動控制（等於可延長或提前關閉報名）；前後端介面（徽章、篩選、按鈕、表單預覽）全部照狀態走。**未做**：草稿狀態與「臨時取消」的獨立標記（可視需要再加，見 8.8） | 無 | 中 |
| ☑ | 報名審核流程 + 候補名單 | 額滿只能拒絕，無法處理「先報名後核准」與遞補 | **v2.20.0 完成**：賽事可設定「⏳ 報名需審核」與「🕒 額滿開放候補」；報名狀態 `pending`／`confirmed`／`waitlisted`／`rejected`；**佔名額＝已核准＋待審核**、候補不佔名額；額滿且開放候補→仍可報名並回報候補順位（沒開候補→400）；管理員逐筆核准／拒絕（可寫原因，兩種結果都通知＋寫稽核）；**有人取消讓出名額 → 第一位候補自動遞補＋推播通知**（需審核的賽事遞補後仍待審核），另有手動「遞補下一位」；待審核／候補者不可編入隊伍。**v2.21.0 補強**：指定遞補（跳過前面的人、被跳過者不受影響）與候補順位手動調整（`waitlist_order`，名單不一致整批拒絕）。**v2.22.0 再補強**：拖拉排序（pointer）、「第 N 位」下拉、順位異動面板（讀稽核日誌）、每賽事遞補通知開關、匯出 CSV 補狀態與順位。**v2.23.0 收尾**：多選一次搬多筆（批次搬移）、異動紀錄往回翻（最多 500 筆）。**未做**：鍵盤拖拉、「把 A 搬到 B 前面」的相對插入、勾選狀態跨重載保留（見 8.8） | 無 | 完成 |
| ☑ | 成績與結果記錄 | 系統目前止於報名，賽後成果無處可放 | **v3.1.0 完成**：管理員登錄名次／成績／狀態／備註＋一鍵自動排名（時間越小越好、分數越大越好、同分並列）；草稿→公布→取消公布；前台領獎台與名次表、自己的名次標記、匯出 CSV、「我的報名」顯示成績；公布時可推播通知參賽者；未公布時對外連統計都不回 | 無 | 中 |
| ☑ | 賽事複製與週期性賽事 | 每月／每週例賽要重複填一次 | **v2.24.0 完成**：📄 一鍵複製（照抄設定、不搬報名與隊伍、複製品不帶週期）；🔁 週期性（每週／每兩週／每月＋結束日，每日排程與「立即建立下一場」共用同一份規則：提前 30 天內、一次只排一場、報名窗一起挪、超過結束日就停、落後回報不亂補）。**未做**：雙週偶數週／每月第幾個星期幾的進階規則、系列一次看全部場次的管理頁（見 8.9） | migration `recurrence` 三欄位（已備） | 完成 |
| ☑ | 場地、地圖與工作人員 | 目前地點只有文字，現場人員分工也無處記錄 | **v2.27.0 完成**：地點加地圖連結（沒填＝用地址自動產生 Google 地圖搜尋連結，舊賽事免補資料；只接受 http/https，不安全的一律 400 說明）；工作人員指派（帳號＋角色：裁判／記錄／攝影，另加醫護／其他；同一場同一人一列、改角色＝更新、備註、指派者；清單公開但只回角色與名稱，指派與移除限管理員以上並留稽核）。**未做**：通知被指派的人（站內公告／推播）、排班表、工作人員名單匯出、「我當工作人員的賽事」清單、一場多地圖 | 無 | 完成 |
| ☑ | 賽事規程附件（PDF） | 規則文件目前只能放外部連結 | **v3.3.0 完成**：一場賽事一份規程 PDF（存資料庫、自家端點供應），卡片「📘 規程」按鈕公開給所有人另開新視窗閱讀、可另存；管理員在編輯表單上傳／更換／移除，皆留稽核；檔案驗檔頭 `%PDF-`、上限 3MB。**未做**：一場多份附件、站內 PDF 渲染（交給瀏覽器） | 無 | 完成 |
| ☑ | 現場報到（檢錄）與簽到〔**第一階段＝管理員勾選制**（你已選 C）〕 | 列印版有簽到欄，但系統裡**沒有「誰到了」的紀錄**：現場靠紙本畫記，事後查不到、也無法阻擋未報到者出賽 | 報名狀態加 `checked_in`／`checked_in_at`／`checked_in_by`，管理員在名單上單手可勾（大按鈕、可搜尋、可只看未到），留稽核；列印簽到表同步帶報到欄。**QR 掃碼報到屬第二階段**（純手寫 QR 產生器＝大，先用編號＋搜尋代替） | **你已決定（2026-09-26）：C＝先做勾選制，QR 掃碼排後續** | 中（第一階段小～中） **v3.6.2 完成**：實作欄位取名 `attended_at`（時間）／`attended_by`（誰勾的）／`onsite`（現場代報名），並在既有 `status` 上限制**只有正取能簽到**（候補／待審核前端不出現、後端回 400 並說明先核准或遞補）。介面在賽事卡片→「報名／隊伍」彈窗最上方：可搜尋、單手按「簽到／取消簽到」、統計「已簽到 X／應到 Y／現場代報名 Z／未到 W」；另加「➕ 現場代報名」（守名額上限、同名不重複、可連結既有帳號）；CSV 補簽到兩欄可列印當紙本備援。稽核 `REGISTER_ATTENDED`／`REGISTER_ATTENDANCE_UNDONE`／`REGISTER_ONSITE`。**QR 掃碼仍屬第二階段**（本版未做） |
| ☐ | 管理員代報名（紙本補登） | 目前只有「本人報名」（`POST /api/competitions/:id/register`）；現場收紙本、或不會用系統的長輩，只能請對方自己想辦法線上報名 | 管理員可代他人建立報名（選既有帳號或輸入臨時姓名），走同一套名額／審核／候補規則，details 記「代報名」並留稽核；名單標示「代報名」以便核對 | 無 | 小～中 |
| ☑ | 賽事取消／延期標記（**v3.6.3 完成**：`competitions` 加 `cancelled_at`／`cancel_reason`／`postponed_date`／`postponed_time`；**原訂時間保留不覆蓋**；卡片標示「⛔ 已取消」／「🕒 延期至…（原訂…）」；取消必填原因且**立刻停止報名**（報名會被擋下並顯示原因）；延期期間暫停報名；三個端點限管理員以上並留稽核；未跑 migration 回 503。**未做**：取消時的一鍵推播公告（需先決定是否新增站台推播開關）） | 狀態目前完全由時間推導；澳門颱風延期是常態，現在只能改時間（看不出「延期過」），也不能標「取消」 | 加 cancel／postpone 標記（狀態列多「已取消」「已延期」、卡片明顯標示、取消時可一鍵推播公告），**保留原訂時間**不覆蓋（同時記延期後的時間） | 無 | 小～中 |
| ☑ | 報名名單列印版 | 現場檢錄需要紙本 | **v3.2.0 完成**：A4 列印預覽（賽事抬頭、正取／候補分段、簽到欄、可含待審核、橫向）；成績表也可列印；列印時只留那張紙；不含個資。（二維碼未做——現場回饋有需要再加） | 無 | 小 |

### 8.4 P2 — 通知與溝通

| 狀態 | 項目 | 為什麼要做 | 具體做法 | 需要你提供 | 工作量 |
|---|---|---|---|---|---|
| ✖ | ~~Email 通知~~ | — | **不做**（使用者 2026-09-25 決定）。通知一律走站內 + Web Push。 | — | — |
| ☑ | 推播時間與事件可設定 | 原本固定每日 01:00（UTC）一次，且只有開賽前提醒 | **v2.25.0 完成**：⏰ 推播設定面板（每日摘要開關／發送時間（澳門時間）／摘要含哪些事件（新發布賽事、即將開賽提醒）／即時通知事件（報名審核結果、候補遞補通知）各自開關）；伺服器每天檢查兩次（01:00／13:00 UTC），一天只發一次、錯過會在稍晚那次補發；被關掉的事件連推播訂閱都不查；讀不到設定一律視為開啟。**未做**：「報名截止前」「開賽前 1 小時」等更細的事件型提醒、時區選擇（目前固定澳門時間）、每位使用者自己的偏好（目前是站台層設定，見 8.10） | 無 | 完成 |
| ☑ | 推播失敗可視與重送 | 目前只看得到成功筆數，失敗時不知原因 | **v2.26.0 完成**：`push_log` 記錄失敗筆數／原因／內容／對象（`failed_count`／`error_detail`／`payload`／`target_user_id`／`target_user_ids`），紀錄列表紅框標示失敗並顯示原因與內容，提供「重送給原本的對象」（只送原本對象、手動不受自動開關影響、寫回重送次數與時間、留稽核，`404／410` 順手停用訂閱，舊紀錄無內容回 400 說明）。**未做**：自動重試（目前一律手動）、失敗通知管理員（下次發每日摘要時附帶失敗摘要） | 無 | 完成 |
| ☑ | 賽事卡片顯示「最新消息」（**v3.6.3 完成**：`competitions.news`／`news_updated_at`；**不需訂閱推播**，公告直接畫在卡片（紅＝取消／琥珀＝延期／藍＝一般消息）與詳情；可清空；更新時間用**本地時區**顯示；內容一律跳脫。**推播仍只當加分項，本版未接**） | 推播的實際觸及極低（線上目前只有 1 則推播、5 個帳號、**0 筆報名**）——沒訂閱的人什麼都看不到，而臨時變更（延期、集合時間、場地更換）最需要人人看得到 | 把該賽事的最新消息（note／公告，含取消、延期、集合時間變更）**直接顯示在卡片與詳情**，不需訂閱；推播只當加分項 | 無 | 小 |
| ☑ | 站內公告 / 訊息中心（優先度提高：取代 Email 通知的角色） | 重要公告目前只能靠推播或口頭；既然不做 Email，站內訊息中心更重要 | **v2.26.0 完成**：管理員發布公告（對象：全部／管理員以上／特定分類訂閱者；置頂、預約發布、結束時間、上架／下架、每則已讀人數），使用者登入在「📣 公告中心」看到（未讀徽章、點擊標記已讀、全部標記已讀、可訂閱公告分類），發布可同時推播（只推給對象使用者）。**未做**：公告附件（圖片／PDF）、公告編輯歷史版本、公告置頂排序拖曳、已讀名單匯出 | 無 | 完成 |

### 8.5 P3 — 使用者體驗與資料

| 狀態 | 項目 | 為什麼要做 | 具體做法 | 需要你提供 | 工作量 |
|---|---|---|---|---|---|
| ☑ | 使用說明與首次使用引導 | 5 個帳號、**0 筆報名**——線上報名這條主線還沒有真人走過，卡在哪裡我們不知道 | 站內一頁式說明：**給參賽者**（怎麼報名、怎麼查自己的報名與成績、手機加入主畫面）與**給管理員**（從建立賽事→開放報名→審核→列印名單→登錄成績的順序清單）；先做文字，必要時再加圖 | 無 | **v3.6.0 完成**：選單「📖 使用說明」16 段（依身分過濾，訪客也看得到）＋管理員操作清單 12 步＋搜尋；內容在 `public/js/guide.js`（前後端測試共用）；現場報到在 v3.6.0 時誠實標示「尚未有系統功能」（v3.6.2 起已改為描述真實功能） |
| ☐ | 使用者個人資料頁 | 每次報名都要重填姓名、單位、聯絡方式 | 個人資料（顯示名稱、單位、聯絡電話、大頭貼）；報名表單自動帶入 | 無 | 中 |
| ☐ | 「我的賽事」儀表板 | 目前只有「我的報名」清單 | 一次看到報名狀態、隊伍、待處理事項、即將開賽倒數 | 無 | 小 |
| ☐ | 使用者自助註冊（可選開關） | 目前靠邀請碼、由管理員建立 | 開放自助註冊 + 管理員審核；可在設定中關閉 | 無 | 中 |
| ☐ | 無障礙與鍵盤操作 | 手機／桌機操作流暢度仍有進步空間 | ARIA 標籤、焦點管理、Esc 關閉彈窗、對比度檢核、跳至主要內容 | 無 | 中 |
| ☐ | 多語介面（繁中 / 簡中 / 英文） | 若有非中文使用者 | 前端字串表 + 語言切換（純手寫、無外部套件） | 是否需要英文／簡中 | 大 |
| ☐ | 深色主題與列印樣式打磨 | 列印與深色下的表格對比可再加強 | 深色表格／徽章調整；列印時強制淺色並隱藏操作按鈕 | 無 | 小 |

### 8.6 P4 — 營運、可觀測與技術債

| 狀態 | 項目 | 為什麼要做 | 具體做法 | 需要你提供 | 工作量 |
|---|---|---|---|---|---|
| ☑ | 後台統計儀表板 | 目前沒有總覽數字 | **v3.0.0 完成**：六個關鍵數字＋三張手寫 SVG 圖（報名／錯誤趨勢、分類分佈，可切 7／14／30 天）＋效能現況（縮圖省下流量、分頁上限、實際查詢耗時、索引數量）；只回聚合數字、缺表降級、快取 60 秒 | 無 | 中 |
| ☑ | API 分頁與查詢優化 | 賽事／報名變多後列表會變慢 | **v3.0.0 完成**：opt-in 分頁（沒帶 limit 行為不變）、`paged_by` 標示 db／memory、單次上限與壞參數保護、`2026-09-27-v3.0.0-perf.sql` 索引、查詢次數守門測試（固定 3 次、不隨資料量成長） | 無 | 中 |
| ☑ | server.js 拆分為模組 | 單檔長到近 7,000 行，維護與測試成本上升 | **v3.4.0 完成**：`server.js` 6,979 → 2,550 行，路由拆成 `routes/` **19 個模組**、依原本順序以 `require('./routes/x')(app, ctx)` 掛載；共用可變狀態集中成一個 `serverState`（否則模組只拿到掛載當下的快照）；新增**路由清單守門測試**（93 條路由＋7 層中間件的數量與順序快照）＋`tests/audit-actions.test.js` 掃描範圍改為 `server.js`＋`routes/*.js`。**未做**：`services/` 再拆一層（目前剩餘量還不需要） | 無 | 完成 |
| ☑ | 瀏覽器檢查腳本納入 repo | 目前放在暫存目錄，會被 24 小時閒置清理刪除（今天就發生過） | **v2.14.0 起完成**：全部檢查都在 `tests/browser/`（`npm run check:browser`／`check:prod`），README 有用法 | 無 | 小 |
| ☑ | 錯誤日誌的截圖上限與列表瘦身 | **新發現（證據）**：`routes/error-logs.js` 只對其他欄位截斷，`screenshot` 原樣入庫（此端點未登入可寫、全域 body 上限 10MB）；`GET /api/admin/error-logs` 用 `select('*')` 連 base64 一起回 → 開一次錯誤日誌等於下載所有截圖 | 截圖上限 300KB（前端先壓縮再送）、列表只回 `has_screenshot`／大小，另開 `GET /api/admin/error-logs/:id/screenshot` 單張取用；舊資料照舊可讀 | 無 | 小 **v3.6.1 完成**：`server.js` 為該路徑單獨掛 512kb body 上限（超過 413）；只接受真正的 `data:image/...;base64,` 且 ≤400,000 字元，不合格只丟圖片、照記錯誤並寫明原因；列表改明確欄位＋`has_screenshot`；新增 `/:id/screenshot`（只有 super_admin／web_owner）供按下時才下載。實測列表回應 **400,974 → 936 位元組**、DOM 內嵌圖片 1→0 |
| ☑ | 補齊 8 條沒有測試的端點 | 用「路由快照 × 測試原始碼」掃描找出來：`POST /api/logs/error`、`PUT /api/auth/change-password`、`POST /api/push/unsubscribe`、`POST /api/admin/login`、回收筒四條（`GET /api/competitions/trash`／`deleted`、`PUT …/:id/restore`、`DELETE …/:id/hard-delete`）完全沒被任何測試或瀏覽器檢查提到——其中**改密碼**與**永久刪除**是最不能出錯的兩條 | **v3.5.4 完成**：3 個新測試檔（`auth-endpoints`／`public-write-endpoints`／`trash-endpoints`），涵蓋權限階梯（401／403／super_admin）、節流 429、五個欄位截斷、`scrypt$` 雜湊儲存、永久刪除真的刪掉資料列、兩個動作都留稽核；每一項都做 RED 驗證（故意改壞實作 → 測試轉紅）。**v3.6.0 已補**：`scripts/test-coverage.js`＋`npm run check:coverage`＋守門測試 `tests/route-coverage.test.js`（92／92） | 無 | 完成 |
| ☑ | 驗證流程不再製造假錯誤日誌 | **新發現（證據）**：線上錯誤日誌 28 筆 `malformed_json_body` 全是 UA=`node`、path=`/api/auth/login`——正是 `tests/browser/prod-smoke.js` 驗「壞 JSON 要回 400」那一步；`check:prod` 每跑一次就多一筆，首頁提示橫幅與巡檢清單被自己的測試灌水，真問題被稀釋。**v3.5.2 完成**：檢查流量帶 `X-CM-Self-Test`（`JWT_SECRET` 簽、`purpose:'self_test'`，不新增環境變數、無法被冒充），伺服器驗簽通過就略過錯誤日誌但**回應完全不變**；沒帶／偽造／亂帶一律照記；自檢流量不再佔用「無效權杖每 IP 每分鐘一筆」的節流額度（否則真實失敗會被靜默吞掉）；`health` 回報 `self_test_skipped`；`check:prod` 新增 2 項斷言（現場送標記過的預期錯誤請求→沒有新增 node/curl 來源日誌＋日誌總數沒增加，實測 33 → 33；**v3.5.3 起**：原「`self_test_skipped >= 1`」那條已廢除——它是單一實例記憶體計數，Vercel 多實例下會誤判，詳見 v3.5.3 列）。**未做**：不刪除既有 29 筆歷史紀錄（已被巡檢標記為已處理，且不會再增加） | 無 | 完成 |
| ☑ | 海報縮圖與列表載入優化 | 大圖會拖慢手機列表 | **v3.0.0 完成**：上傳時瀏覽器 canvas 產生縮圖（無伺服器端影像套件）、`?variant=thumb` 沒有縮圖自動退回原圖、卡片 `loading="lazy"` 且視窗外不下載、分享列印仍用原圖 | 無 | 中 |

### 8.7 暫緩或不建議（附理由）

| 項目 | 決定 | 理由 |
|---|---|---|
| 引入前端框架（React / Vue） | 不建議 | 與嚴格 CSP（script-src 'self'）及「純手寫、無外部套件」的既有慣例衝突，目前規模不需要 |
| 用 Supabase Storage 存海報 | 不建議 | anon key 無法建立 bucket（已改為存資料庫、由自家端點供應，效果相同） |
| 對 anon key 開放 RLS policy | 不建議 | 等於把資料庫直接暴露給任何拿到金鑰的人（正解見 P0 的 service_role 升級） |
| 手機原生 App（iOS / Android） | 暫緩 | PWA 已可加入主畫面全螢幕使用；維護兩套程式成本高 |
| 線上繳費 / 金流串接 | 暫緩 | 需要金流帳號與法遵流程，若無實際收費需求不急 |
| 自動化爬蟲接入外部賽事 | 暫緩 | 需先確認目標網站條款與資料格式（見第七節第 6 項） |

### 8.8 我會持續發掘的方向

除了上面已列項目，我每次發版都會一併回報新發現並更新本節，來源包括：

| 來源 | 我會注意什麼 |
|---|---|
| 線上錯誤日誌（含來源分析） | 反覆出現的錯誤類型；**來源是真人還是我們自己的測試**（本輪就抓到 28 筆其實是自己的煙霧測試造成的） |
| 路由快照 × 測試覆蓋率掃描 | 用 `tests/fixtures/route-inventory.json` 對測試原始碼做比對，找出「完全沒被任何測試提到的端點」（本輪找出 8 條） |
| 程式碼巡檢 | 重複邏輯、**缺乏上限的輸入**（例如錯誤截圖）、可簡化處 |
| **真實使用數據**（`GET /api/admin/stats`） | 帳號／賽事／報名／推播的實際數字（本輪：5 帳號、4 賽事、**0 筆報名**、1 則推播）——決定優先度的是「能不能被真的用完一次」，不是功能數量 |
| 你的實際使用 | 你提到的每一次「這裡怪怪的」都是最準的需求來源 |

> 不再以截圖作為發掘來源（使用者 2026-09-26 指示「不要保留任何screenshot」）：版面與視覺問題改用**數值量測**（`tests/browser/layout-check.js`、`live-style-check.js` 的計算值）與你的口頭描述判斷。

### 8.9 本輪（v3.4.0 之後）的發現與建議順序

**先看真實數據再談優先度**（2026-09-26 由 `GET /api/admin/stats?days=30` 讀回，非估算）：

| 項目 | 實際數字 | 對規劃的意義 |
|---|---|---|
| 賽事 | 4 場（1 進行中、3 報名已截止） | 已經在用，但規模小 |
| **報名** | **0 筆**（已核准／待審核／候補全部 0） | **線上報名這條主線還沒被真人走過**——現在加再多功能，都不如讓它被完整走一遍 |
| 帳號 | 5 個（30 天內活躍 3 個） | 使用者少，介面說明比自動化更重要 |
| 推播 | 1 則、送出 3 筆、成功率 100% | 技術沒問題，**觸及率極低**（沒有訂閱就收不到） |
| 錯誤日誌 | 30 天內 0 筆未處理 | 系統體質健康；剩下的多是我們自己測試造成的雜訊（見 8.6） |

**建議順序（投資報酬率排序）**：

| 順序 | 項目 | 為什麼排這裡 | 工作量 |
|---|---|---|---|
| 1 | ~~**CORS 改白名單**（8.2）~~ **✅ v3.5.0 完成**（連同 JWT 改 HttpOnly cookie＋CSRF） | ZAP 實測確認過寬；改動只有一行＋守門測試，卻是最明確的一個安全設定瑕疵——先清掉它 | 小 |
| 2 | ~~使用說明＋管理員操作清單（8.5）~~ **✅ v3.6.0 完成** | 0 筆報名的根因可能是「不知道怎麼用」；成本最低、對真實使用影響最大 | 小 |
| 3 | ~~驗證流程不再製造假錯誤日誌（8.6）~~ **✅ v3.5.2 完成** | 讓提示橫幅與巡檢清單恢復可信——這是後續所有判斷的基礎 | 小 |
| 4 | ~~選單彈窗在電腦上太細（使用者 2026-09-27 反映）~~ **✅ v3.5.4 完成** | 使用者實際看到的問題優先處理（先量測再改，並把「太細」變成可量測的斷言） | 小 |
| 4b | ~~錯誤日誌截圖上限與列表瘦身（8.6）~~ **✅ v3.6.1 完成** | 未登入可寫的端點卻沒有大小上限，是現存最明顯的資源風險 | 小 |
| 5 | ~~補齊 8 條沒有測試的端點（8.6）~~ **✅ v3.5.4 完成** | 改密碼與永久刪除沒有測試，是不能出錯的地方 | 小～中 |
| 6 | ~~現場報到〔第一階段＝勾選制〕＋管理員代報名（8.3）~~ **✅ v3.6.2 完成** | 實際辦一場就會用到；有了真實資料，後面的規劃才有依據 | 中 |
| 7 | 賽事取消／延期＋卡片最新消息（8.3／8.4） | 臨時變更的溝通不該依賴推播訂閱 | 小～中 |
| 8 | ~~JWT 存放位置~~ **✅ v3.5.0 完成**、QR 掃碼報到、P3 使用者體驗項目 | **你已指示「其他排期後續更新」**——依這份順序往後排 | 視選項 |

> 使用者 2026-09-26 指示：現場報到採 **(C) 先做管理員勾選制**，其餘項目**排期後續更新**。

**需要你決定的事**（不影響我先做上面 1～4 項）：

| 問題 | 選項 |
|---|---|
| ~~現場報到方式~~ | **已決定（2026-09-26）：C＝先做管理員勾選制，QR 掃碼排後續更新** |
| 是否先做「示範賽事＋測試報名」 | 讓流程被完整走過一次（可隨時刪除），或直接用真實報名活動試跑 |
| 自助註冊要不要開放 | 目前靠管理員建立帳號；開放會改變對外性質（見 8.5） |
| 賽事取消的語意 | 只標「已取消」保留原時間，或同時支援「延期到新時間」並保留原訂時間 |
| ~~JWT 存放位置（ZAP 中風險）~~ | **已決定（2026-09-26）：(C) 改 httpOnly cookie＋CSRF，v3.5.0 已完成** |
| ~~要不要換 `JWT_SECRET`~~ | **已決定：不換**（報告已刪除，權杖到期即失效） |

### 8.10 ZAP 掃描結果判讀（2026-09-26，使用者提供報告）

掃描設定：ZAP 2.17.0、**被動掃描**（passive，只觀察既有流量、沒有主動攻擊）、共 17 種警示、68 個實例；兩個「站台」被納入報告。

| 站台 | 高 | 中 | 低 | 資訊 |
|---|---|---|---|---|
| `https://competition-manager-hazel.vercel.app`（我們） | 0 | **2 種（12 個實例中 2 類）** | 0 | 12 |
| `https://update.googleapis.com`（**第三方，Chrome 更新服務，不是我們的**） | 0 | 0 | 1 | 2 |

**結論：我們這邊 0 高風險、沒有任何「需要立即處理」的漏洞。**

**要處理的（已排入 8.2 P0）**：

| 警示 | 風險 | 判讀 | 處置 |
|---|---|---|---|
| 跨域配置錯誤（CORS） | 中 | **是真的**：`app.use('/api', cors())` 讓所有 API 回應帶 `Access-Control-Allow-Origin: *`。因為我們用 `Authorization` 標頭而非 cookie，第三方拿不到權杖 → 只能讀本來就公開的資料（賽事、成績、公告），**實際影響低，但屬明確過寬** | 改白名單（同源前端不需要 CORS，所以無相容性風險）＋守門測試 |
| ~~JWT 存在瀏覽器 localStorage~~ | 已修 | **v3.5.0 已改為 HttpOnly cookie**：回應不再回傳 token、前端不寫入 `localStorage`、`document.cookie` 也讀不到（瀏覽器檢查會驗這三點） | 完成 |

**已評估、判定為誤報或可接受（不處理，理由留檔）**：

| 警示 | 數量 | 為什麼不處理 |
|---|---|---|
| Information Disclosure - Suspicious Comments | 8 | 命中我們的註解（例如 `// db = 由資料庫切片` 被 `\bDB\b` 命中）。內容是設計說明，**不含任何憑證、路徑或帳號**；註解是這個專案的文件來源，不打算為了掃描器而拿掉 |
| Information Disclosure - Information in Browser localStorage | 4 | 是主題偏好等 UI 設定（`cm-theme`…）與顯示用的 `competition_user`（帳號名稱／角色，不含憑證），不含個資；權杖已於 v3.5.0 移出 localStorage |
| Re-examine Cache-control Directives／Retrieved from Cache | 6／6 | 都是**靜態檔**（`/js/*.js`、`/css/*.css`）被 CDN 快取，本來就該快取；API 回應已一律 `Cache-Control: no-store`（實測確認） |
| Strict-Transport-Security Header Not Set（低） | 1 | **不是我們的站台**（`update.googleapis.com`）；我們的回應實測有 `Strict-Transport-Security: max-age=31536000; includeSubDomains` |
| Tech Detected（HSTS／HTTP/3／OpenGSE／PWA／Tailwind／Vercel） | 7 | 技術指紋，不是問題 |

**掃描衛生（下次掃描前請調整）**：

1. **把範圍限制在自家網域**：這次報告裡有 `update.googleapis.com`（Chrome 自動更新的服務）——掃描第三方服務可能違反對方條款，那份結果也與我們無關（唯一的「低風險」就是它）。
2. 保持**被動掃描**即可：被動掃描不會對正式站送出攻擊性請求，適合現在的正式環境。
3. **報告不要進 repo**：本次報告內含登入請求（明碼密碼）與有效權杖，一旦提交就等於把憑證寫進版本歷史（撤不掉）。要留存就放在 repo 外、加密保存。

## 九、自動偵測到的問題（待修復清單）

> 本節由 `scripts/error-log-triage.js` 自動產生並覆寫（最後更新：2026/9/27 01:36:40）。資料來源：線上錯誤日誌（透過自家 API 讀取）。
> 狀態欄的「新增」代表我還沒看到你決定優先度；「已排入 Roadmap」代表第八節已有對應項目。

> 🏷️ 本輪巡檢已把讀到的 0 筆未處理紀錄標記為已處理（rimuru）。首頁提示橫幅只會針對「下一批新錯誤」再提醒。

### 9.1 需要處理的問題

| 狀態 | 優先 | 錯誤類型 | 次數 | 未處理 | 近 24 小時 | 最後發生 | 建議動作 | 範例訊息 |
|---|---|---|---|---|---|---|---|---|
| 已排入 Roadmap | 高 | `unhandled_server_error` | 1 | 0 | 0 | 2026/9/24 01:36:44 | 看範例訊息定位來源；若重複出現，加入單元測試防回歸 | Unexpected token 'b', "{"username": broken" is not valid JSON |
| 已排入 Roadmap | 中 | `js_runtime_issue` | 1 | 0 | 0 | 2026/9/24 01:15:06 | 依裝置與訊息判斷前端相容性問題 | Script error. |

### 9.2 低優先／僅供觀察

| 錯誤類型 | 次數 | 最後發生 | 說明 |
|---|---|---|---|
| `malformed_json_body` | 29 | 2026/9/26 | 用戶端請求錯誤（v2.14.0 已分流為 4xx）（多為正常現象，累積過多再處理） |
| `auth_invalid_token` | 2 | 2026/9/26 | 權杖無效／過期（多為正常現象，累積過多再處理） |

**統計**：共 4 種錯誤類型、33 筆紀錄，其中未處理 0 筆。
